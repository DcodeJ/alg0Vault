/* Hand-authored Python 3 and C++17 references for all 100 library entries and 24 patterns. */
window.ALG0_LANGUAGE_SAMPLES = {
  "patterns": {
    "sliding-window": {
      "python": "def longest_unique(s):\n    seen = set()\n    left = best = 0\n    for right, char in enumerate(s):\n        while char in seen:\n            seen.remove(s[left])\n            left += 1\n        seen.add(char)\n        best = max(best, right - left + 1)\n    return best",
      "cpp": "// Byte-oriented strings; use ASCII input for character comparisons.\nint longestUnique(const string& s) {\n    unordered_set<char> seen;\n    size_t left = 0, best = 0;\n    for (size_t right = 0; right < s.size(); ++right) {\n        while (seen.count(s[right])) seen.erase(s[left++]);\n        seen.insert(s[right]);\n        best = max(best, right - left + 1);\n    }\n    return static_cast<int>(best);\n}"
    },
    "fixed-window": {
      "python": "def max_window_sum(a, k):\n    if not 1 <= k <= len(a):\n        raise ValueError(\"k must be between 1 and len(a)\")\n    total = sum(a[i] for i in range(k))\n    best = total\n    for right in range(k, len(a)):\n        total += a[right] - a[right - k]\n        best = max(best, total)\n    return best",
      "cpp": "long long maxWindowSum(const vector<int>& a, int k) {\n    if (k < 1 || k > static_cast<int>(a.size())) throw invalid_argument(\"invalid k\");\n    long long sum = accumulate(a.begin(), a.begin() + k, 0LL), best = sum;\n    for (size_t right = k; right < a.size(); ++right) {\n        sum += static_cast<long long>(a[right]) - a[right - k];\n        best = max(best, sum);\n    }\n    return best;\n}"
    },
    "two-pointers": {
      "python": "def sorted_pair(a, target):\n    left, right = 0, len(a) - 1\n    while left < right:\n        total = a[left] + a[right]\n        if total == target:\n            return [left, right]\n        if total < target:\n            left += 1\n        else:\n            right -= 1\n    return []",
      "cpp": "vector<int> sortedPair(const vector<int>& a, long long target) {\n    int left = 0, right = static_cast<int>(a.size()) - 1;\n    while (left < right) {\n        long long sum = static_cast<long long>(a[left]) + a[right];\n        if (sum == target) return {left, right};\n        if (sum < target) ++left; else --right;\n    }\n    return {};\n}"
    },
    "hash-map": {
      "python": "def two_sum(a, target):\n    index = {}\n    for i, value in enumerate(a):\n        if target - value in index:\n            return [index[target - value], i]\n        index[value] = i\n    return []",
      "cpp": "vector<int> twoSum(const vector<int>& a, long long target) {\n    unordered_map<long long, int> index;\n    for (int i = 0; i < static_cast<int>(a.size()); ++i) {\n        auto found = index.find(target - a[i]);\n        if (found != index.end()) return {found->second, i};\n        index[a[i]] = i;\n    }\n    return {};\n}"
    },
    "prefix-sum": {
      "python": "def count_target_sums(a, target):\n    frequency = {0: 1}\n    prefix = count = 0\n    for value in a:\n        prefix += value\n        count += frequency.get(prefix - target, 0)\n        frequency[prefix] = frequency.get(prefix, 0) + 1\n    return count",
      "cpp": "long long countTargetSums(const vector<int>& a, long long target) {\n    unordered_map<long long, long long> frequency{{0, 1}};\n    long long prefix = 0, count = 0;\n    for (int value : a) {\n        prefix += value;\n        auto found = frequency.find(prefix - target);\n        if (found != frequency.end()) count += found->second;\n        ++frequency[prefix];\n    }\n    return count;\n}"
    },
    "fast-slow": {
      "python": "class Node:\n    def __init__(self, value=0, next=None):\n        self.value = value\n        self.next = next\n\ndef has_cycle(head):\n    slow = fast = head\n    while fast is not None and fast.next is not None:\n        slow = slow.next\n        fast = fast.next.next\n        if slow is fast:\n            return True\n    return False",
      "cpp": "// Node pointers are non-owning; caller manages node lifetime.\nstruct Node { int value = 0; Node* next = nullptr; };\nbool hasCycle(Node* head) {\n    Node *slow = head, *fast = head;\n    while (fast && fast->next) {\n        slow = slow->next;\n        fast = fast->next->next;\n        if (slow == fast) return true;\n    }\n    return false;\n}"
    },
    "linked-reversal": {
      "python": "class Node:\n    def __init__(self, value=0, next=None):\n        self.value = value\n        self.next = next\n\ndef reverse(head):\n    previous = None\n    while head is not None:\n        next_node = head.next\n        head.next = previous\n        previous, head = head, next_node\n    return previous",
      "cpp": "// Node pointers are non-owning; caller manages node lifetime.\nstruct Node { int value = 0; Node* next = nullptr; };\nNode* reverseList(Node* head) {\n    Node* previous = nullptr;\n    while (head) {\n        Node* next = head->next;\n        head->next = previous;\n        previous = head;\n        head = next;\n    }\n    return previous;\n}"
    },
    "intervals": {
      "python": "def merge_intervals(intervals):\n    result = []\n    for start, end in sorted(intervals):\n        if not result or result[-1][1] < start:\n            result.append([start, end])\n        else:\n            result[-1][1] = max(result[-1][1], end)\n    return result",
      "cpp": "vector<array<int, 2>> mergeIntervals(vector<array<int, 2>> intervals) {\n    sort(intervals.begin(), intervals.end());\n    vector<array<int, 2>> result;\n    for (auto interval : intervals) {\n        if (result.empty() || result.back()[1] < interval[0]) result.push_back(interval);\n        else result.back()[1] = max(result.back()[1], interval[1]);\n    }\n    return result;\n}"
    },
    "binary-search": {
      "python": "def lower_bound(a, target):\n    left, right = 0, len(a)\n    while left < right:\n        mid = left + (right - left) // 2\n        if a[mid] < target:\n            left = mid + 1\n        else:\n            right = mid\n    return left",
      "cpp": "int lowerBound(const vector<int>& a, int target) {\n    int left = 0, right = static_cast<int>(a.size());\n    while (left < right) {\n        int mid = left + (right - left) / 2;\n        if (a[mid] < target) left = mid + 1; else right = mid;\n    }\n    return left;\n}"
    },
    "answer-search": {
      "python": "def min_capacity(weights, days):\n    if days < 1 or any(w <= 0 for w in weights):\n        raise ValueError(\"positive weights and days required\")\n    if not weights:\n        return 0\n    low, high = max(weights), sum(weights)\n    while low < high:\n        mid = low + (high - low) // 2\n        used, load = 1, 0\n        for weight in weights:\n            if load + weight > mid:\n                used += 1\n                load = 0\n            load += weight\n        if used <= days:\n            high = mid\n        else:\n            low = mid + 1\n    return low",
      "cpp": "long long minCapacity(const vector<int>& weights, int days) {\n    if (days < 1 || any_of(weights.begin(), weights.end(), [](int w){ return w <= 0; }))\n        throw invalid_argument(\"positive weights and days required\");\n    if (weights.empty()) return 0;\n    long long low = *max_element(weights.begin(), weights.end());\n    long long high = accumulate(weights.begin(), weights.end(), 0LL);\n    while (low < high) {\n        long long mid = low + (high - low) / 2, load = 0;\n        int used = 1;\n        for (int weight : weights) {\n            if (load + weight > mid) { ++used; load = 0; }\n            load += weight;\n        }\n        if (used <= days) high = mid; else low = mid + 1;\n    }\n    return low;\n}"
    },
    "monotonic-stack": {
      "python": "def warmer_days(temperatures):\n    pending = []\n    answer = [0] * len(temperatures)\n    for i, value in enumerate(temperatures):\n        while pending and value > temperatures[pending[-1]]:\n            previous = pending.pop()\n            answer[previous] = i - previous\n        pending.append(i)\n    return answer",
      "cpp": "vector<int> warmerDays(const vector<int>& temperatures) {\n    vector<int> pending, answer(temperatures.size());\n    for (int i = 0; i < static_cast<int>(temperatures.size()); ++i) {\n        while (!pending.empty() && temperatures[i] > temperatures[pending.back()]) {\n            int previous = pending.back(); pending.pop_back();\n            answer[previous] = i - previous;\n        }\n        pending.push_back(i);\n    }\n    return answer;\n}"
    },
    "monotonic-deque": {
      "python": "from collections import deque\n\ndef window_maximum(a, k):\n    if not 1 <= k <= len(a):\n        raise ValueError(\"invalid k\")\n    candidates, result = deque(), []\n    for i, value in enumerate(a):\n        while candidates and candidates[0] <= i - k:\n            candidates.popleft()\n        while candidates and a[candidates[-1]] <= value:\n            candidates.pop()\n        candidates.append(i)\n        if i >= k - 1:\n            result.append(a[candidates[0]])\n    return result",
      "cpp": "vector<int> windowMaximum(const vector<int>& a, int k) {\n    if (k < 1 || k > static_cast<int>(a.size())) throw invalid_argument(\"invalid k\");\n    deque<int> candidates;\n    vector<int> result;\n    for (int i = 0; i < static_cast<int>(a.size()); ++i) {\n        while (!candidates.empty() && candidates.front() <= i - k) candidates.pop_front();\n        while (!candidates.empty() && a[candidates.back()] <= a[i]) candidates.pop_back();\n        candidates.push_back(i);\n        if (i >= k - 1) result.push_back(a[candidates.front()]);\n    }\n    return result;\n}"
    },
    "tree-dfs": {
      "python": "class Node:\n    def __init__(self, value=0, left=None, right=None):\n        self.value, self.left, self.right = value, left, right\n\n# Deep trees may exceed Python's recursion limit; use an explicit stack then.\ndef depth(root):\n    if root is None:\n        return 0\n    return 1 + max(depth(root.left), depth(root.right))",
      "cpp": "// Caller owns nodes. Deep trees may exhaust the call stack.\nstruct Node { int value = 0; Node *left = nullptr, *right = nullptr; };\nint depth(Node* root) {\n    return root ? 1 + max(depth(root->left), depth(root->right)) : 0;\n}"
    },
    "bfs": {
      "python": "from collections import deque\n\n# graph[u] lists valid neighbor indices; source is a valid vertex.\ndef distances(graph, source):\n    distance = [-1] * len(graph)\n    distance[source] = 0\n    queue = deque([source])\n    while queue:\n        node = queue.popleft()\n        for neighbor in graph[node]:\n            if distance[neighbor] == -1:\n                distance[neighbor] = distance[node] + 1\n                queue.append(neighbor)\n    return distance",
      "cpp": "// Valid vertex indices are required.\nvector<int> distances(const vector<vector<int>>& graph, int source) {\n    vector<int> distance(graph.size(), -1);\n    queue<int> pending;\n    distance.at(source) = 0; pending.push(source);\n    while (!pending.empty()) {\n        int node = pending.front(); pending.pop();\n        for (int next : graph[node]) if (distance[next] == -1) {\n            distance[next] = distance[node] + 1;\n            pending.push(next);\n        }\n    }\n    return distance;\n}"
    },
    "topological": {
      "python": "from collections import deque\n\ndef topological_order(graph):\n    indegree = [0] * len(graph)\n    for edges in graph:\n        for v in edges:\n            indegree[v] += 1\n    queue = deque(v for v, degree in enumerate(indegree) if degree == 0)\n    order = []\n    while queue:\n        u = queue.popleft()\n        order.append(u)\n        for v in graph[u]:\n            indegree[v] -= 1\n            if indegree[v] == 0:\n                queue.append(v)\n    return order if len(order) == len(graph) else []",
      "cpp": "vector<int> topologicalOrder(const vector<vector<int>>& graph) {\n    vector<int> indegree(graph.size()), order;\n    for (const auto& edges : graph) for (int v : edges) ++indegree[v];\n    queue<int> ready;\n    for (int v = 0; v < static_cast<int>(graph.size()); ++v)\n        if (indegree[v] == 0) ready.push(v);\n    while (!ready.empty()) {\n        int u = ready.front(); ready.pop(); order.push_back(u);\n        for (int v : graph[u]) if (--indegree[v] == 0) ready.push(v);\n    }\n    return order.size() == graph.size() ? order : vector<int>{};\n}"
    },
    "union-find": {
      "python": "class DisjointSet:\n    def __init__(self, n):\n        self.parent = list(range(n))\n        self.size = [1] * n\n\n    def find(self, x):\n        while self.parent[x] != x:\n            self.parent[x] = self.parent[self.parent[x]]\n            x = self.parent[x]\n        return x\n\n    def union(self, a, b):\n        a, b = self.find(a), self.find(b)\n        if a == b:\n            return False\n        if self.size[a] < self.size[b]:\n            a, b = b, a\n        self.parent[b] = a\n        self.size[a] += self.size[b]\n        return True",
      "cpp": "class DisjointSet {\n    vector<int> parent, size;\npublic:\n    explicit DisjointSet(int n) : parent(n), size(n, 1) { iota(parent.begin(), parent.end(), 0); }\n    int find(int x) {\n        while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; }\n        return x;\n    }\n    bool unite(int a, int b) {\n        a = find(a); b = find(b);\n        if (a == b) return false;\n        if (size[a] < size[b]) swap(a, b);\n        parent[b] = a; size[a] += size[b];\n        return true;\n    }\n};"
    },
    "dijkstra": {
      "python": "import heapq\nfrom math import inf\n\n# graph[u] contains (neighbor, nonnegative_weight) pairs.\ndef shortest_paths(graph, source):\n    distance = [inf] * len(graph)\n    distance[source] = 0\n    heap = [(0, source)]\n    while heap:\n        cost, u = heapq.heappop(heap)\n        if cost != distance[u]:\n            continue\n        for v, weight in graph[u]:\n            if weight < 0:\n                raise ValueError(\"negative edge weight\")\n            candidate = cost + weight\n            if candidate < distance[v]:\n                distance[v] = candidate\n                heapq.heappush(heap, (candidate, v))\n    return distance",
      "cpp": "// Valid indices and nonnegative weights; LLONG_MAX denotes unreachable.\nvector<long long> shortestPaths(const vector<vector<pair<int,int>>>& graph, int source) {\n    const auto INF = numeric_limits<long long>::max();\n    vector<long long> distance(graph.size(), INF);\n    priority_queue<pair<long long,int>, vector<pair<long long,int>>, greater<pair<long long,int>>> heap;\n    distance.at(source) = 0; heap.push({0, source});\n    while (!heap.empty()) {\n        auto [cost, u] = heap.top(); heap.pop();\n        if (cost != distance[u]) continue;\n        for (auto [v, weight] : graph[u]) {\n            if (weight < 0) throw invalid_argument(\"negative edge weight\");\n            if (cost > INF - weight) throw overflow_error(\"distance overflow\");\n            long long candidate = cost + weight;\n            if (candidate < distance[v]) {\n                distance[v] = candidate; heap.push({candidate, v});\n            }\n        }\n    }\n    return distance;\n}"
    },
    "heap": {
      "python": "import heapq\n\ndef kth_largest(a, k):\n    if not 1 <= k <= len(a):\n        raise ValueError(\"invalid k\")\n    heap = []\n    for value in a:\n        heapq.heappush(heap, value)\n        if len(heap) > k:\n            heapq.heappop(heap)\n    return heap[0]",
      "cpp": "int kthLargest(const vector<int>& a, int k) {\n    if (k < 1 || k > static_cast<int>(a.size())) throw invalid_argument(\"invalid k\");\n    priority_queue<int, vector<int>, greater<int>> heap;\n    for (int value : a) {\n        heap.push(value);\n        if (heap.size() > static_cast<size_t>(k)) heap.pop();\n    }\n    return heap.top();\n}"
    },
    "backtracking": {
      "python": "# Distinct values; duplicate inputs produce duplicate subsets.\ndef subsets(a):\n    result, path = [], []\n    def search(index):\n        if index == len(a):\n            result.append(path.copy())\n            return\n        search(index + 1)\n        path.append(a[index])\n        search(index + 1)\n        path.pop()\n    search(0)\n    return result",
      "cpp": "vector<vector<int>> subsets(const vector<int>& a) {\n    vector<vector<int>> result;\n    vector<int> path;\n    function<void(size_t)> search = [&](size_t index) {\n        if (index == a.size()) { result.push_back(path); return; }\n        search(index + 1);\n        path.push_back(a[index]); search(index + 1); path.pop_back();\n    };\n    search(0);\n    return result;\n}"
    },
    "dp-1d": {
      "python": "def rob(houses):\n    two_back = previous = 0\n    for value in houses:\n        two_back, previous = previous, max(previous, two_back + value)\n    return previous",
      "cpp": "long long rob(const vector<int>& houses) {\n    long long twoBack = 0, previous = 0;\n    for (int value : houses) {\n        long long current = max(previous, twoBack + value);\n        twoBack = previous; previous = current;\n    }\n    return previous;\n}"
    },
    "dp-2d": {
      "python": "def lcs_length(a, b):\n    dp = [[0] * (len(b) + 1) for _ in range(len(a) + 1)]\n    for i in range(1, len(a) + 1):\n        for j in range(1, len(b) + 1):\n            dp[i][j] = (dp[i-1][j-1] + 1 if a[i-1] == b[j-1]\n                        else max(dp[i-1][j], dp[i][j-1]))\n    return dp[len(a)][len(b)]",
      "cpp": "int lcsLength(const string& a, const string& b) {\n    vector<vector<int>> dp(a.size() + 1, vector<int>(b.size() + 1));\n    for (size_t i = 1; i <= a.size(); ++i)\n        for (size_t j = 1; j <= b.size(); ++j)\n            dp[i][j] = a[i-1] == b[j-1] ? dp[i-1][j-1] + 1 : max(dp[i-1][j], dp[i][j-1]);\n    return dp[a.size()][b.size()];\n}"
    },
    "greedy": {
      "python": "# Nonnegative jump lengths.\ndef can_reach_end(jumps):\n    if not jumps:\n        return False\n    farthest = 0\n    for i, value in enumerate(jumps):\n        if i > farthest:\n            return False\n        farthest = max(farthest, i + value)\n        if farthest >= len(jumps) - 1:\n            return True\n    return False",
      "cpp": "// Nonnegative jump lengths.\nbool canReachEnd(const vector<int>& jumps) {\n    if (jumps.empty()) return false;\n    long long farthest = 0;\n    for (int i = 0; i < static_cast<int>(jumps.size()); ++i) {\n        if (i > farthest) return false;\n        farthest = max(farthest, static_cast<long long>(i) + jumps[i]);\n        if (farthest >= static_cast<long long>(jumps.size()) - 1) return true;\n    }\n    return false;\n}"
    },
    "trie": {
      "python": "class Trie:\n    def __init__(self):\n        self.children = {}\n        self.word = False\n\n    def insert(self, text):\n        node = self\n        for char in text:\n            if char not in node.children:\n                node.children[char] = Trie()\n            node = node.children[char]\n        node.word = True\n\n    def _walk(self, text):\n        node = self\n        for char in text:\n            node = node.children.get(char)\n            if node is None:\n                return None\n        return node\n\n    def search(self, text):\n        node = self._walk(text)\n        return node is not None and node.word\n\n    def starts_with(self, prefix):\n        return self._walk(prefix) is not None",
      "cpp": "class Trie {\n    unordered_map<char, unique_ptr<Trie>> children;\n    bool word = false;\n    const Trie* walk(const string& text) const {\n        auto node = this;\n        for (char c : text) {\n            auto found = node->children.find(c);\n            if (found == node->children.end()) return nullptr;\n            node = found->second.get();\n        }\n        return node;\n    }\npublic:\n    void insert(const string& text) {\n        auto node = this;\n        for (char c : text) {\n            auto& child = node->children[c];\n            if (!child) child = make_unique<Trie>();\n            node = child.get();\n        }\n        node->word = true;\n    }\n    bool search(const string& text) const { auto node = walk(text); return node && node->word; }\n    bool startsWith(const string& prefix) const { return walk(prefix) != nullptr; }\n};"
    },
    "bitwise": {
      "python": "# One value occurs once; all other values occur exactly twice.\ndef single_number(a):\n    result = 0\n    for value in a:\n        result ^= value\n    return result",
      "cpp": "// One value occurs once; all other values occur exactly twice.\nint singleNumber(const vector<int>& a) {\n    int result = 0;\n    for (int value : a) result ^= value;\n    return result;\n}"
    }
  },
  "algorithms": {
    "Binary Search": {
      "python": "def binary_search(a, target):\n    left, right = 0, len(a) - 1\n    while left <= right:\n        mid = left + (right - left) // 2\n        if a[mid] == target:\n            return mid\n        if a[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1",
      "cpp": "int binarySearch(const vector<int>& a, int target) {\n    int left = 0, right = static_cast<int>(a.size()) - 1;\n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        if (a[mid] == target) return mid;\n        if (a[mid] < target) left = mid + 1; else right = mid - 1;\n    }\n    return -1;\n}"
    },
    "Upper Bound": {
      "python": "def upper_bound(a, target):\n    left, right = 0, len(a)\n    while left < right:\n        mid = (left + right) // 2\n        if a[mid] <= target:\n            left = mid + 1\n        else:\n            right = mid\n    return left",
      "cpp": "int upperBound(const vector<int>& a, int target) {\n    int left = 0, right = static_cast<int>(a.size());\n    while (left < right) {\n        int mid = left + (right - left) / 2;\n        if (a[mid] <= target) left = mid + 1; else right = mid;\n    }\n    return left;\n}"
    },
    "Bubble Sort": {
      "python": "def bubble_sort(a):\n    for end in range(len(a) - 1, 0, -1):\n        swapped = False\n        for i in range(end):\n            if a[i] > a[i + 1]:\n                a[i], a[i + 1] = a[i + 1], a[i]\n                swapped = True\n        if not swapped:\n            break\n    return a",
      "cpp": "void bubbleSort(vector<int>& a) {\n    for (size_t end = a.size(); end > 1; --end) {\n        bool swapped = false;\n        for (size_t i = 1; i < end; ++i)\n            if (a[i-1] > a[i]) { swap(a[i-1], a[i]); swapped = true; }\n        if (!swapped) break;\n    }\n}"
    },
    "Selection Sort": {
      "python": "def selection_sort(a):\n    for i in range(len(a)):\n        smallest = i\n        for j in range(i + 1, len(a)):\n            if a[j] < a[smallest]:\n                smallest = j\n        a[i], a[smallest] = a[smallest], a[i]\n    return a",
      "cpp": "void selectionSort(vector<int>& a) {\n    for (size_t i = 0; i < a.size(); ++i) {\n        size_t smallest = i;\n        for (size_t j = i + 1; j < a.size(); ++j)\n            if (a[j] < a[smallest]) smallest = j;\n        swap(a[i], a[smallest]);\n    }\n}"
    },
    "Insertion Sort": {
      "python": "def insertion_sort(a):\n    for i in range(1, len(a)):\n        value, j = a[i], i - 1\n        while j >= 0 and a[j] > value:\n            a[j + 1] = a[j]\n            j -= 1\n        a[j + 1] = value\n    return a",
      "cpp": "void insertionSort(vector<int>& a) {\n    for (size_t i = 1; i < a.size(); ++i) {\n        int value = a[i];\n        size_t j = i;\n        while (j > 0 && a[j-1] > value) { a[j] = a[j-1]; --j; }\n        a[j] = value;\n    }\n}"
    },
    "Merge Sort": {
      "python": "def merge_sort(a):\n    def sort(left, right):\n        if right - left < 2:\n            return\n        mid = (left + right) // 2\n        sort(left, mid)\n        sort(mid, right)\n        merged = []\n        i, j = left, mid\n        while i < mid and j < right:\n            if a[i] <= a[j]:\n                merged.append(a[i])\n                i += 1\n            else:\n                merged.append(a[j])\n                j += 1\n        merged.extend(a[i:mid])\n        merged.extend(a[j:right])\n        a[left:right] = merged\n    sort(0, len(a))\n    return a",
      "cpp": "void mergeSort(vector<int>& a) {\n    vector<int> buffer(a.size());\n    function<void(size_t,size_t)> sortRange = [&](size_t left, size_t right) {\n        if (right - left < 2) return;\n        size_t mid = left + (right - left) / 2;\n        sortRange(left, mid); sortRange(mid, right);\n        size_t i = left, j = mid, k = left;\n        while (i < mid && j < right) buffer[k++] = a[i] <= a[j] ? a[i++] : a[j++];\n        while (i < mid) buffer[k++] = a[i++];\n        while (j < right) buffer[k++] = a[j++];\n        for (k = left; k < right; ++k) a[k] = buffer[k];\n    };\n    sortRange(0, a.size());\n}"
    },
    "Quick Sort": {
      "python": "# Last-element pivot: average O(n log n), worst O(n^2).\n# Explicit stack avoids Python's recursion limit.\ndef quick_sort(a):\n    stack = [(0, len(a) - 1)]\n    while stack:\n        left, right = stack.pop()\n        if left >= right:\n            continue\n        pivot, cut = a[right], left\n        for i in range(left, right):\n            if a[i] <= pivot:\n                a[cut], a[i] = a[i], a[cut]\n                cut += 1\n        a[cut], a[right] = a[right], a[cut]\n        stack.append((left, cut - 1))\n        stack.append((cut + 1, right))\n    return a",
      "cpp": "// Last-element pivot; worst-case O(n^2) time and O(n) stack space.\nvoid quickSort(vector<int>& a) {\n    vector<pair<int,int>> pending{{0, static_cast<int>(a.size()) - 1}};\n    while (!pending.empty()) {\n        auto [left, right] = pending.back(); pending.pop_back();\n        if (left >= right) continue;\n        int pivot = a[right], cut = left;\n        for (int i = left; i < right; ++i) if (a[i] <= pivot) swap(a[cut++], a[i]);\n        swap(a[cut], a[right]);\n        pending.push_back({left, cut - 1}); pending.push_back({cut + 1, right});\n    }\n}"
    },
    "Heap Sort": {
      "python": "def heap_sort(a):\n    def sift(root, end):\n        while 2 * root + 1 < end:\n            child = 2 * root + 1\n            if child + 1 < end and a[child] < a[child + 1]:\n                child += 1\n            if a[root] >= a[child]:\n                return\n            a[root], a[child] = a[child], a[root]\n            root = child\n    for root in range(len(a) // 2 - 1, -1, -1):\n        sift(root, len(a))\n    for end in range(len(a) - 1, 0, -1):\n        a[0], a[end] = a[end], a[0]\n        sift(0, end)\n    return a",
      "cpp": "void heapSort(vector<int>& a) {\n    auto sift = [&](size_t root, size_t end) {\n        while (root < end / 2) {\n            size_t child = 2 * root + 1;\n            if (child + 1 < end && a[child] < a[child+1]) ++child;\n            if (a[root] >= a[child]) return;\n            swap(a[root], a[child]); root = child;\n        }\n    };\n    for (size_t i = a.size() / 2; i > 0; --i) sift(i - 1, a.size());\n    for (size_t end = a.size(); end > 1; --end) {\n        swap(a[0], a[end - 1]); sift(0, end - 1);\n    }\n}"
    },
    "Counting Sort": {
      "python": "# Nonnegative values with a reasonably small maximum.\ndef counting_sort(a):\n    if any(x < 0 for x in a):\n        raise ValueError(\"nonnegative values required\")\n    if not a:\n        return a\n    counts = [0] * (max(a) + 1)\n    for value in a:\n        counts[value] += 1\n    index = 0\n    for value, count in enumerate(counts):\n        for _ in range(count):\n            a[index] = value\n            index += 1\n    return a",
      "cpp": "// Nonnegative values; O(max(a)) extra memory, so keep the value range small.\nvoid countingSort(vector<int>& a) {\n    if (a.empty()) return;\n    if (*min_element(a.begin(), a.end()) < 0) throw invalid_argument(\"nonnegative values required\");\n    vector<size_t> counts(static_cast<size_t>(*max_element(a.begin(), a.end())) + 1);\n    for (int value : a) ++counts[value];\n    size_t index = 0;\n    for (size_t value = 0; value < counts.size(); ++value)\n        for (size_t i = 0; i < counts[value]; ++i) a[index++] = static_cast<int>(value);\n}"
    },
    "Radix Sort": {
      "python": "# Base-10 LSD radix sort for nonnegative integers.\ndef radix_sort(a):\n    if any(x < 0 for x in a):\n        raise ValueError(\"nonnegative values required\")\n    maximum = max(a, default=0)\n    place = 1\n    while maximum // place:\n        buckets = [[] for _ in range(10)]\n        for value in a:\n            buckets[(value // place) % 10].append(value)\n        a[:] = [value for bucket in buckets for value in bucket]\n        place *= 10\n    return a",
      "cpp": "void radixSort(vector<int>& a) {\n    if (a.empty()) return;\n    if (*min_element(a.begin(), a.end()) < 0) throw invalid_argument(\"nonnegative values required\");\n    int maximum = *max_element(a.begin(), a.end());\n    for (long long place = 1; maximum / place > 0; place *= 10) {\n        array<vector<int>,10> buckets;\n        for (int value : a) buckets[(value / place) % 10].push_back(value);\n        size_t i = 0;\n        for (const auto& bucket : buckets) for (int value : bucket) a[i++] = value;\n    }\n}"
    },
    "Kadane's Algorithm": {
      "python": "def max_subarray(a):\n    if not a:\n        raise ValueError(\"nonempty input required\")\n    ending = best = a[0]\n    for i in range(1, len(a)):\n        value = a[i]\n        ending = max(value, ending + value)\n        best = max(best, ending)\n    return best",
      "cpp": "long long maxSubarray(const vector<int>& a) {\n    if (a.empty()) throw invalid_argument(\"nonempty input required\");\n    long long ending = a[0], best = a[0];\n    for (size_t i = 1; i < a.size(); ++i) {\n        ending = max(static_cast<long long>(a[i]), ending + a[i]);\n        best = max(best, ending);\n    }\n    return best;\n}"
    },
    "Longest Increasing Subsequence": {
      "python": "from bisect import bisect_left\n\ndef lis_length(a):\n    tails = []\n    for value in a:\n        i = bisect_left(tails, value)\n        if i == len(tails):\n            tails.append(value)\n        else:\n            tails[i] = value\n    return len(tails)",
      "cpp": "int lisLength(const vector<int>& a) {\n    vector<int> tails;\n    for (int value : a) {\n        auto it = lower_bound(tails.begin(), tails.end(), value);\n        if (it == tails.end()) tails.push_back(value); else *it = value;\n    }\n    return static_cast<int>(tails.size());\n}"
    },
    "Prefix Sum Range Query": {
      "python": "def prefix_sums(a):\n    prefix = [0]\n    for value in a:\n        prefix.append(prefix[-1] + value)\n    return prefix\n\n# Inclusive query [left, right]; build prefix_sums once.\ndef range_sum(prefix, left, right):\n    if not 0 <= left <= right < len(prefix) - 1:\n        raise ValueError(\"invalid inclusive range\")\n    return prefix[right + 1] - prefix[left]",
      "cpp": "vector<long long> prefixSums(const vector<int>& a) {\n    vector<long long> prefix(1, 0);\n    for (int value : a) prefix.push_back(checkedAdd(prefix.back(), value));\n    return prefix;\n}\n// Inclusive query [left, right].\nlong long rangeSum(const vector<long long>& prefix, int left, int right) {\n    if (left < 0 || left > right || right >= static_cast<int>(prefix.size())-1)\n        throw invalid_argument(\"invalid inclusive range\");\n    // Prefix differences must fit long long.\n    long long a = prefix[right+1], b = prefix[left];\n    if ((b > 0 && a < numeric_limits<long long>::min()+b) ||\n        (b < 0 && a > numeric_limits<long long>::max()+b)) throw overflow_error(\"range sum\");\n    return a-b;\n}"
    },
    "Depth-First Search": {
    "python": "def dfs(graph, source):\n    # Iterator frames preserve recursive preorder with O(V) extra space.\n    if not 0 <= source < len(graph):\n        raise ValueError(\"Invalid source\")\n    seen, order = {source}, [source]\n    stack = [iter(graph[source])]\n    while stack:\n        v = next(stack[-1], None)\n        if v is None:\n            stack.pop()\n        elif not 0 <= v < len(graph):\n            raise ValueError(\"Invalid vertex\")\n        elif v not in seen:\n            seen.add(v)\n            order.append(v)\n            stack.append(iter(graph[v]))\n    return order",
    "cpp": "// Iterator frames preserve recursive preorder with O(V) extra space.\nvector<int> dfs(const vector<vector<int>>& graph, int source) {\n    if (source < 0 || source >= static_cast<int>(graph.size()))\n        throw invalid_argument(\"Invalid source\");\n    vector<bool> seen(graph.size());\n    vector<int> order{source};\n    vector<pair<int,size_t>> stack{{source,0}};\n    seen[source] = true;\n    while (!stack.empty()) {\n        int u = stack.back().first;\n        if (stack.back().second == graph[u].size()) {\n            stack.pop_back();\n            continue;\n        }\n        int v = graph[u][stack.back().second++];\n        if (v < 0 || v >= static_cast<int>(graph.size()))\n            throw invalid_argument(\"Invalid vertex\");\n        if (!seen[v]) {\n            seen[v] = true;\n            order.push_back(v);\n            stack.emplace_back(v,0);\n        }\n    }\n    return order;\n}"
    },
    "Kruskal's Minimum Spanning Tree": {
      "python": "# Edges are (u, v, weight). Return total MST weight, or None if disconnected.\ndef kruskal(n, edges):\n    if n < 0:\n        raise ValueError(\"negative vertex count\")\n    edges = list(edges)\n    if any(not (0 <= u < n and 0 <= v < n) for u,v,w in edges):\n        raise ValueError(\"invalid vertex\")\n    parent, size = list(range(n)), [1]*n\n    def find(x):\n        while parent[x] != x:\n            parent[x] = parent[parent[x]]\n            x = parent[x]\n        return x\n    total = used = 0\n    for u,v,w in sorted(edges, key=lambda edge: edge[2]):\n        a,b = find(u),find(v)\n        if a == b:\n            continue\n        if size[a] < size[b]:\n            a,b = b,a\n        parent[b] = a\n        size[a] += size[b]\n        total += w\n        used += 1\n    return total if used == max(0,n-1) else None",
      "cpp": "// Edge {u,v,weight}; nullopt means disconnected.\noptional<long long> kruskal(int n, vector<array<int,3>> edges) {\n    if (n < 0) throw invalid_argument(\"negative vertex count\");\n    for (auto e : edges) if (e[0]<0 || e[1]<0 || e[0]>=n || e[1]>=n) throw invalid_argument(\"vertex\");\n    vector<int> parent(n), size(n,1); iota(parent.begin(),parent.end(),0);\n    auto find = [&](int x) { while (parent[x]!=x) { parent[x]=parent[parent[x]]; x=parent[x]; } return x; };\n    sort(edges.begin(),edges.end(),[](auto a,auto b){return a[2]<b[2];});\n    long long total=0; int used=0;\n    for (auto e : edges) {\n        int a=find(e[0]), b=find(e[1]);\n        if (a==b) continue;\n        if (size[a]<size[b]) swap(a,b);\n        parent[b]=a; size[a]+=size[b]; total=checkedAdd(total,e[2]); ++used;\n    }\n    return used==max(0,n-1) ? optional<long long>(total) : nullopt;\n}"
    },
    "Binary Tree Level Order": {
      "python": "class Node:\n    def __init__(self, value=0, left=None, right=None):\n        self.value, self.left, self.right = value, left, right\n\nfrom collections import deque\n\ndef level_order(root):\n    if root is None:\n        return []\n    queue, result = deque([root]), []\n    while queue:\n        level = []\n        for _ in range(len(queue)):\n            node = queue.popleft()\n            level.append(node.value)\n            if node.left is not None:\n                queue.append(node.left)\n            if node.right is not None:\n                queue.append(node.right)\n        result.append(level)\n    return result",
      "cpp": "// Non-owning links; caller manages node lifetime.\nstruct Node { int value=0; Node *left=nullptr, *right=nullptr; };\nvector<vector<int>> levelOrder(Node* root) {\n    if (!root) return {};\n    queue<Node*> q; q.push(root);\n    vector<vector<int>> result;\n    while (!q.empty()) {\n        size_t count=q.size(); vector<int> level;\n        while (count--) {\n            Node* node=q.front(); q.pop(); level.push_back(node->value);\n            if (node->left) q.push(node->left);\n            if (node->right) q.push(node->right);\n        }\n        result.push_back(move(level));\n    }\n    return result;\n}"
    },
    "Lowest Common Ancestor": {
      "python": "class Node:\n    def __init__(self, value=0, left=None, right=None):\n        self.value, self.left, self.right = value, left, right\n\n# Both target node objects must exist in this binary tree.\ndef lowest_common_ancestor(root, p, q):\n    if root is None or root is p or root is q:\n        return root\n    left = lowest_common_ancestor(root.left,p,q)\n    right = lowest_common_ancestor(root.right,p,q)\n    return root if left is not None and right is not None else (left or right)",
      "cpp": "// Non-owning links; caller manages node lifetime.\nstruct Node { int value=0; Node *left=nullptr, *right=nullptr; };\n// Both targets must be in the tree. Deep trees can exhaust the call stack.\nNode* lowestCommonAncestor(Node* root, Node* p, Node* q) {\n    if (!root || root==p || root==q) return root;\n    Node* left=lowestCommonAncestor(root->left,p,q);\n    Node* right=lowestCommonAncestor(root->right,p,q);\n    return left && right ? root : (left ? left : right);\n}"
    },
    "KMP String Search": {
      "python": "def kmp_search(text, pattern):\n    if not pattern:\n        return 0\n    lps = [0]*len(pattern)\n    matched = 0\n    for i in range(1,len(pattern)):\n        while matched and pattern[i] != pattern[matched]:\n            matched = lps[matched-1]\n        if pattern[i] == pattern[matched]:\n            matched += 1\n        lps[i] = matched\n    matched = 0\n    for i,char in enumerate(text):\n        while matched and char != pattern[matched]:\n            matched = lps[matched-1]\n        if char == pattern[matched]:\n            matched += 1\n        if matched == len(pattern):\n            return i-len(pattern)+1\n    return -1",
      "cpp": "// Byte offsets for std::string; empty pattern matches at 0.\nint kmpSearch(const string& text, const string& pattern) {\n    if (pattern.empty()) return 0;\n    vector<int> lps(pattern.size()); int matched=0;\n    for (int i=1;i<static_cast<int>(pattern.size());++i) {\n        while (matched && pattern[i]!=pattern[matched]) matched=lps[matched-1];\n        if (pattern[i]==pattern[matched]) ++matched;\n        lps[i]=matched;\n    }\n    matched=0;\n    for (int i=0;i<static_cast<int>(text.size());++i) {\n        while (matched && text[i]!=pattern[matched]) matched=lps[matched-1];\n        if (text[i]==pattern[matched]) ++matched;\n        if (matched==static_cast<int>(pattern.size())) return i-matched+1;\n    }\n    return -1;\n}"
    },
    "Coin Change": {
      "python": "def coin_change(coins, amount):\n    if amount < 0 or any(c <= 0 for c in coins):\n        raise ValueError(\"positive coins and nonnegative amount required\")\n    dp = [0]+[amount+1]*amount\n    for value in range(1,amount+1):\n        for coin in coins:\n            if coin <= value:\n                dp[value] = min(dp[value],dp[value-coin]+1)\n    return dp[amount] if dp[amount] <= amount else -1",
    "cpp": "int coinChange(const vector<int>& coins, int amount) {\n    if (amount<0 || amount==numeric_limits<int>::max()) throw invalid_argument(\"amount too large\");\n    for (int c:coins) if(c<=0) throw invalid_argument(\"positive coins required\");\n    vector<int> dp(static_cast<size_t>(amount)+1,amount+1); dp[0]=0;\n    for (int v=1;v<=amount;++v) for (int c:coins)\n        if (c<=v && dp[v-c]<=amount) dp[v]=min(dp[v],dp[v-c]+1);\n    return dp[amount]<=amount ? dp[amount] : -1;\n}"
    },
    "0/1 Knapsack": {
      "python": "def knapsack(weights, values, capacity):\n    if capacity < 0 or len(weights)!=len(values) or any(w<0 for w in weights):\n        raise ValueError(\"invalid capacity or weights\")\n    dp = [0]*(capacity+1)\n    for weight,value in zip(weights,values):\n        for c in range(capacity,weight-1,-1):\n            dp[c] = max(dp[c],dp[c-weight]+value)\n    return dp[capacity]",
      "cpp": "long long knapsack(const vector<int>& weights, const vector<int>& values, int capacity) {\n    if (capacity<0 || weights.size()!=values.size()) throw invalid_argument(\"input\");\n    vector<long long> dp(static_cast<size_t>(capacity)+1);\n    for (size_t i=0;i<weights.size();++i) {\n        if (weights[i]<0) throw invalid_argument(\"weight\");\n        for (int c=capacity;c>=weights[i];--c) dp[c]=max(dp[c],checkedAdd(dp[c-weights[i]],values[i]));\n    }\n    return dp[capacity];\n}"
    },
    "Generate Permutations": {
      "python": "# Returns unique permutations even when input contains duplicates.\ndef permutations(a):\n    a = sorted(a)\n    used, path, result = [False]*len(a), [], []\n    def visit():\n        if len(path)==len(a):\n            result.append(path.copy())\n            return\n        for i,value in enumerate(a):\n            if used[i] or (i and a[i]==a[i-1] and not used[i-1]):\n                continue\n            used[i]=True\n            path.append(value)\n            visit()\n            path.pop()\n            used[i]=False\n    visit()\n    return result",
      "cpp": "vector<vector<int>> permutations(vector<int> a) {\n    sort(a.begin(),a.end());\n    vector<bool> used(a.size()); vector<int> path; vector<vector<int>> result;\n    function<void()> visit=[&]() {\n        if(path.size()==a.size()) {result.push_back(path);return;}\n        for(size_t i=0;i<a.size();++i) {\n            if(used[i] || (i && a[i]==a[i-1] && !used[i-1])) continue;\n            used[i]=true;path.push_back(a[i]);visit();path.pop_back();used[i]=false;\n        }\n    };\n    visit();return result;\n}"
    },
    "Next Greater Element": {
      "python": "# Return next strictly greater values, or -1 if none exists.\ndef next_greater(a):\n    stack, result = [], [-1]*len(a)\n    for i,value in enumerate(a):\n        while stack and a[stack[-1]] < value:\n            result[stack.pop()] = value\n        stack.append(i)\n    return result",
      "cpp": "vector<int> nextGreater(const vector<int>& a) {\n    vector<int> pending,result(a.size(),-1);\n    for(int i=0;i<static_cast<int>(a.size());++i) {\n        while(!pending.empty() && a[pending.back()]<a[i]) {result[pending.back()]=a[i];pending.pop_back();}\n        pending.push_back(i);\n    }\n    return result;\n}"
    },
    "Top K Frequent Elements": {
      "python": "from collections import Counter\nimport heapq\n\n# Ties prefer the larger value. Results are ordered by frequency, then value.\ndef top_k_frequent(a, k):\n    counts = Counter(a)\n    if not 0 <= k <= len(counts):\n        raise ValueError(\"invalid k\")\n    heap = []\n    for value,count in counts.items():\n        heapq.heappush(heap,(count,value))\n        if len(heap)>k:\n            heapq.heappop(heap)\n    return [value for count,value in sorted(heap,reverse=True)]",
      "cpp": "vector<int> topKFrequent(const vector<int>& a, int k) {\n    unordered_map<int,int> counts;\n    for(int value:a) ++counts[value];\n    if(k<0 || k>static_cast<int>(counts.size())) throw invalid_argument(\"k\");\n    priority_queue<pair<int,int>,vector<pair<int,int>>,greater<pair<int,int>>> heap;\n    for(auto [value,count]:counts) {heap.push({count,value});if(static_cast<int>(heap.size())>k)heap.pop();}\n    vector<int> result;\n    while(!heap.empty()) {result.push_back(heap.top().second);heap.pop();}\n    reverse(result.begin(),result.end());return result;\n}"
    },
    "Detect Directed Cycle": {
      "python": "# Iterative DFS: 0=unseen, 1=active, 2=finished. Valid neighbor indices required.\ndef has_directed_cycle(graph):\n    color = [0]*len(graph)\n    for start in range(len(graph)):\n        if color[start]:\n            continue\n        color[start]=1\n        stack=[(start,iter(graph[start]))]\n        while stack:\n            u,neighbors=stack[-1]\n            v=next(neighbors,None)\n            if v is None:\n                color[u]=2\n                stack.pop()\n            elif color[v]==1:\n                return True\n            elif color[v]==0:\n                color[v]=1\n                stack.append((v,iter(graph[v])))\n    return False",
      "cpp": "bool hasDirectedCycle(const vector<vector<int>>& graph) {\n    vector<int> color(graph.size());\n    for(int start=0;start<static_cast<int>(graph.size());++start) {\n        if(color[start]) continue;\n        color[start]=1; vector<pair<int,size_t>> stack{{start,0}};\n        while(!stack.empty()) {\n            int u=stack.back().first;\n            if(stack.back().second==graph[u].size()) {color[u]=2;stack.pop_back();continue;}\n            int v=graph[u][stack.back().second++];\n            if(color[v]==1) return true;\n            if(color[v]==0) {color[v]=1;stack.push_back({v,0});}\n        }\n    }\n    return false;\n}"
    },
    "Valid Parentheses": {
      "python": "def valid_parentheses(text):\n    pairs={')':'(',']':'[','}':'{'}\n    stack=[]\n    for char in text:\n        if char in '([{':\n            stack.append(char)\n        elif char not in pairs or not stack or stack.pop()!=pairs[char]:\n            return False\n    return not stack",
      "cpp": "bool validParentheses(const string& text) {\n    vector<char> stack;\n    for(char c:text) {\n        if(c=='(' || c=='[' || c=='{') stack.push_back(c);\n        else {\n            char expected=c==')'?'(':c==']'?'[':c=='}'?'{':'\\0';\n            if(!expected || stack.empty() || stack.back()!=expected) return false;\n            stack.pop_back();\n        }\n    }\n    return stack.empty();\n}"
    },
    "Valid Anagram": {
      "python": "from collections import Counter\n\ndef valid_anagram(a,b):\n    return Counter(a)==Counter(b)",
      "cpp": "// Compares byte counts (ASCII recommended); Python compares Unicode code points.\nbool validAnagram(const string& a,const string& b) {\n    array<long long,256> count{};\n    for(unsigned char c:a) ++count[c];\n    for(unsigned char c:b) --count[c];\n    return all_of(count.begin(),count.end(),[](long long n){return n==0;});\n}"
    },
    "Group Anagrams": {
      "python": "def group_anagrams(words):\n    groups={}\n    for word in words:\n        key=''.join(sorted(word))\n        groups.setdefault(key,[]).append(word)\n    return list(groups.values())",
      "cpp": "vector<vector<string>> groupAnagrams(const vector<string>& words) {\n    unordered_map<string,size_t> index; vector<vector<string>> groups;\n    for(const auto& word:words) {\n        string key=word;sort(key.begin(),key.end());\n        auto [it,inserted]=index.emplace(key,groups.size());\n        if(inserted) groups.push_back({});\n        groups[it->second].push_back(word);\n    }\n    return groups;\n}"
    },
    "Valid Palindrome": {
      "python": "# ASCII letters/digits; punctuation and letter case are ignored.\ndef valid_palindrome(text):\n    def alnum(c):\n        return 'a'<=c<='z' or 'A'<=c<='Z' or '0'<=c<='9'\n    left,right=0,len(text)-1\n    while left<right:\n        if not alnum(text[left]):\n            left+=1\n        elif not alnum(text[right]):\n            right-=1\n        elif text[left].lower()!=text[right].lower():\n            return False\n        else:\n            left+=1\n            right-=1\n    return True",
      "cpp": "// ASCII letters/digits; no locale-dependent Unicode classification.\nbool validPalindrome(const string& text) {\n    auto alnum=[](char c){return (c>='a'&&c<='z')||(c>='A'&&c<='Z')||(c>='0'&&c<='9');};\n    auto lower=[](char c){return c>='A'&&c<='Z'?char(c-'A'+'a'):c;};\n    int l=0,r=static_cast<int>(text.size())-1;\n    while(l<r) {\n        if(!alnum(text[l])) ++l;\n        else if(!alnum(text[r])) --r;\n        else if(lower(text[l++])!=lower(text[r--])) return false;\n    }\n    return true;\n}"
    },
    "Merge Two Sorted Lists": {
      "python": "class Node:\n    def __init__(self,value=0,next=None):\n        self.value,self.next=value,next\n\n# Relinks two disjoint, sorted, acyclic lists.\ndef merge_lists(a,b):\n    dummy=tail=Node()\n    while a is not None and b is not None:\n        if a.value<=b.value:\n            tail.next,a=a,a.next\n        else:\n            tail.next,b=b,b.next\n        tail=tail.next\n    tail.next=a if a is not None else b\n    return dummy.next",
      "cpp": "// Non-owning links; caller owns all nodes, including removed nodes.\nstruct Node {int value=0;Node* next=nullptr;};\nNode* mergeLists(Node* a,Node* b) {\n    Node dummy;Node* tail=&dummy;\n    while(a && b) {\n        if(a->value<=b->value) {tail->next=a;a=a->next;}\n        else {tail->next=b;b=b->next;}\n        tail=tail->next;\n    }\n    tail->next=a?a:b;return dummy.next;\n}"
    },
    "Remove Nth Node From End": {
      "python": "class Node:\n    def __init__(self,value=0,next=None):\n        self.value,self.next=value,next\n\ndef remove_nth(head,n):\n    if n<=0:\n        raise ValueError(\"n must be positive\")\n    dummy=Node(next=head)\n    fast=slow=dummy\n    for _ in range(n):\n        fast=fast.next\n        if fast is None:\n            raise ValueError(\"n exceeds length\")\n    while fast.next is not None:\n        fast,slow=fast.next,slow.next\n    slow.next=slow.next.next\n    return dummy.next",
      "cpp": "// Non-owning links; caller owns all nodes, including removed nodes.\nstruct Node {int value=0;Node* next=nullptr;};\nNode* removeNth(Node* head,int n) {\n    if(n<=0) throw invalid_argument(\"n\");\n    Node dummy{0,head};Node *fast=&dummy,*slow=&dummy;\n    for(int i=0;i<n;++i) {fast=fast->next;if(!fast)throw invalid_argument(\"n exceeds length\");}\n    while(fast->next){fast=fast->next;slow=slow->next;}\n    slow->next=slow->next->next;return dummy.next;\n}"
    },
    "Three Sum": {
      "python": "def three_sum(values):\n    a=sorted(values)\n    result=[]\n    for i in range(len(a)-2):\n        if i and a[i]==a[i-1]:\n            continue\n        left,right=i+1,len(a)-1\n        while left<right:\n            total=a[i]+a[left]+a[right]\n            if total<0:\n                left+=1\n            elif total>0:\n                right-=1\n            else:\n                result.append([a[i],a[left],a[right]])\n                left+=1\n                right-=1\n                while left<right and a[left]==a[left-1]:\n                    left+=1\n                while left<right and a[right]==a[right+1]:\n                    right-=1\n    return result",
      "cpp": "vector<vector<int>> threeSum(vector<int> a) {\n    sort(a.begin(),a.end());vector<vector<int>> result;\n    for(int i=0;i+2<static_cast<int>(a.size());++i) {\n        if(i && a[i]==a[i-1])continue;\n        int l=i+1,r=static_cast<int>(a.size())-1;\n        while(l<r) {\n            long long sum=static_cast<long long>(a[i])+a[l]+a[r];\n            if(sum<0)++l;else if(sum>0)--r;\n            else {result.push_back({a[i],a[l],a[r]});++l;--r;\n                while(l<r&&a[l]==a[l-1])++l;while(l<r&&a[r]==a[r+1])--r;}\n        }\n    }\n    return result;\n}"
    },
    "Container With Most Water": {
      "python": "def max_water(height):\n    if any(h<0 for h in height):\n        raise ValueError(\"nonnegative heights required\")\n    left,right,best=0,len(height)-1,0\n    while left<right:\n        best=max(best,(right-left)*min(height[left],height[right]))\n        if height[left]<height[right]:\n            left+=1\n        else:\n            right-=1\n    return best",
      "cpp": "long long maxWater(const vector<int>& height) {\n    for(int h:height)if(h<0)throw invalid_argument(\"height\");\n    int l=0,r=static_cast<int>(height.size())-1;long long best=0;\n    while(l<r) {\n        best=max(best,static_cast<long long>(r-l)*min(height[l],height[r]));\n        if(height[l]<height[r])++l;else --r;\n    }\n    return best;\n}"
    },
    "Trapping Rain Water": {
      "python": "def trapped_water(height):\n    if any(h<0 for h in height):\n        raise ValueError(\"nonnegative heights required\")\n    left,right=0,len(height)-1\n    left_max=right_max=total=0\n    while left<=right:\n        if left_max<=right_max:\n            left_max=max(left_max,height[left])\n            total+=left_max-height[left]\n            left+=1\n        else:\n            right_max=max(right_max,height[right])\n            total+=right_max-height[right]\n            right-=1\n    return total",
      "cpp": "long long trappedWater(const vector<int>& height) {\n    for(int h:height)if(h<0)throw invalid_argument(\"height\");\n    int l=0,r=static_cast<int>(height.size())-1,leftMax=0,rightMax=0;\n    long long total=0;\n    while(l<=r) {\n        if(leftMax<=rightMax){leftMax=max(leftMax,height[l]);total+=leftMax-height[l++];}\n        else {rightMax=max(rightMax,height[r]);total+=rightMax-height[r--];}\n    }\n    return total;\n}"
    },
    "Product of Array Except Self": {
      "python": "def product_except_self(a):\n    result=[1]*len(a)\n    prefix=1\n    for i,value in enumerate(a):\n        result[i]=prefix\n        prefix*=value\n    suffix=1\n    for i in range(len(a)-1,-1,-1):\n        result[i]*=suffix\n        suffix*=a[i]\n    return result",
      "cpp": "// Throws if an intermediate product does not fit long long.\nvector<long long> productExceptSelf(const vector<int>& a) {\n    vector<long long> result(a.size(),1);\n    long long prefix=1,suffix=1;\n    for(size_t i=0;i<a.size();++i){result[i]=prefix;if(i+1<a.size())prefix=checkedMultiply(prefix,a[i]);}\n    for(size_t i=a.size();i>0;--i){result[i-1]=checkedMultiply(result[i-1],suffix);if(i>1)suffix=checkedMultiply(suffix,a[i-1]);}\n    return result;\n}"
    },
    "Rotate Array": {
      "python": "# In-place right rotation; negative k rotates left.\ndef rotate_array(a,k):\n    if not a:\n        return a\n    k%=len(a)\n    def reverse(left,right):\n        while left<right:\n            a[left],a[right]=a[right],a[left]\n            left+=1\n            right-=1\n    reverse(0,len(a)-1)\n    reverse(0,k-1)\n    reverse(k,len(a)-1)\n    return a",
      "cpp": "void rotateArray(vector<int>& a,long long k) {\n    if(a.empty())return;\n    long long n=static_cast<long long>(a.size());k%=n;if(k<0)k+=n;\n    reverse(a.begin(),a.end());reverse(a.begin(),a.begin()+k);reverse(a.begin()+k,a.end());\n}"
    },
    "Longest Consecutive Sequence": {
      "python": "def longest_consecutive(a):\n    values=set(a)\n    best=0\n    for value in values:\n        if value-1 not in values:\n            end=value\n            while end+1 in values:\n                end+=1\n            best=max(best,end-value+1)\n    return best",
      "cpp": "int longestConsecutive(const vector<int>& a) {\n    unordered_set<long long> values(a.begin(),a.end());long long best=0;\n    for(long long value:values)if(!values.count(value-1)) {\n        long long end=value;while(values.count(end+1))++end;best=max(best,end-value+1);\n    }\n    return static_cast<int>(best);\n}"
    },
    "Best Time to Buy and Sell Stock": {
      "python": "def max_profit(prices):\n    cheapest=None\n    best=0\n    for price in prices:\n        cheapest=price if cheapest is None else min(cheapest,price)\n        best=max(best,price-cheapest)\n    return best",
      "cpp": "long long maxProfit(const vector<int>& prices) {\n    long long cheapest=numeric_limits<long long>::max(),best=0;\n    for(int price:prices){cheapest=min(cheapest,static_cast<long long>(price));best=max(best,price-cheapest);}\n    return best;\n}"
    },
    "Binary Tree Inorder Traversal": {
      "python": "class Node:\n    def __init__(self, value=0, left=None, right=None):\n        self.value, self.left, self.right = value, left, right\n\ndef inorder(root):\n    stack,result=[],[]\n    while root is not None or stack:\n        while root is not None:\n            stack.append(root)\n            root=root.left\n        root=stack.pop()\n        result.append(root.value)\n        root=root.right\n    return result",
      "cpp": "// Non-owning links; caller manages node lifetime.\nstruct Node { int value=0; Node *left=nullptr, *right=nullptr; };\nvector<int> inorder(Node* root) {\n    vector<Node*> stack;vector<int> result;\n    while(root || !stack.empty()) {\n        while(root){stack.push_back(root);root=root->left;}\n        root=stack.back();stack.pop_back();result.push_back(root->value);root=root->right;\n    }\n    return result;\n}"
    },
    "Validate Binary Search Tree": {
      "python": "class Node:\n    def __init__(self, value=0, left=None, right=None):\n        self.value, self.left, self.right = value, left, right\n\n# Strict BST: duplicate values are not allowed.\ndef valid_bst(root):\n    stack=[]\n    previous=None\n    while root is not None or stack:\n        while root is not None:\n            stack.append(root)\n            root=root.left\n        root=stack.pop()\n        if previous is not None and root.value<=previous:\n            return False\n        previous=root.value\n        root=root.right\n    return True",
      "cpp": "// Non-owning links; caller manages node lifetime.\nstruct Node { int value=0; Node *left=nullptr, *right=nullptr; };\nbool validBst(Node* root) {\n    vector<Node*> stack;optional<int> previous;\n    while(root || !stack.empty()) {\n        while(root){stack.push_back(root);root=root->left;}\n        root=stack.back();stack.pop_back();\n        if(previous && root->value<=*previous)return false;\n        previous=root->value;root=root->right;\n    }\n    return true;\n}"
    },
    "Diameter of Binary Tree": {
      "python": "class Node:\n    def __init__(self, value=0, left=None, right=None):\n        self.value, self.left, self.right = value, left, right\n\n# Diameter is measured in edges. Explicit postorder avoids recursion limits.\ndef diameter(root):\n    if root is None:\n        return 0\n    heights={}\n    stack=[(root,False)]\n    best=0\n    while stack:\n        node,done=stack.pop()\n        if node is None:\n            continue\n        if not done:\n            stack.extend([(node,True),(node.right,False),(node.left,False)])\n        else:\n            left,right=heights.get(node.left,0),heights.get(node.right,0)\n            best=max(best,left+right)\n            heights[node]=1+max(left,right)\n    return best",
      "cpp": "// Non-owning links; caller manages node lifetime.\nstruct Node { int value=0; Node *left=nullptr, *right=nullptr; };\n// Iterative postorder, O(n) extra space. Diameter is in edges.\nint diameter(Node* root) {\n    unordered_map<Node*,int> height;vector<pair<Node*,bool>> stack{{root,false}};int best=0;\n    while(!stack.empty()) {\n        auto [node,done]=stack.back();stack.pop_back();if(!node)continue;\n        if(!done){stack.push_back({node,true});stack.push_back({node->right,false});stack.push_back({node->left,false});}\n        else {int l=node->left?height[node->left]:0,r=node->right?height[node->right]:0;best=max(best,l+r);height[node]=1+max(l,r);}\n    }\n    return best;\n}"
    },
    "Number of Islands": {
      "python": "# Rectangular grid of '0'/'1' strings. Does not mutate the input.\ndef count_islands(grid):\n    if not grid:\n        return 0\n    rows,cols=len(grid),len(grid[0])\n    if any(len(row)!=cols or any(c not in '01' for c in row) for row in grid):\n        raise ValueError(\"rectangular binary grid required\")\n    seen=set()\n    count=0\n    for r in range(rows):\n        for c in range(cols):\n            if grid[r][c]!='1' or (r,c) in seen:\n                continue\n            count+=1\n            stack=[(r,c)]\n            seen.add((r,c))\n            while stack:\n                y,x=stack.pop()\n                for dy,dx in ((1,0),(-1,0),(0,1),(0,-1)):\n                    ny,nx=y+dy,x+dx\n                    if 0<=ny<rows and 0<=nx<cols and grid[ny][nx]=='1' and (ny,nx) not in seen:\n                        seen.add((ny,nx))\n                        stack.append((ny,nx))\n    return count",
      "cpp": "// Copies the rectangular binary grid, leaving the caller's grid unchanged.\nint countIslands(vector<string> grid) {\n    if(grid.empty())return 0;\n    int rows=static_cast<int>(grid.size()),cols=static_cast<int>(grid[0].size()),count=0;\n    for(const auto& row:grid)if(row.size()!=static_cast<size_t>(cols)||row.find_first_not_of(\"01\")!=string::npos)throw invalid_argument(\"grid\");\n    const int dy[]={1,-1,0,0},dx[]={0,0,1,-1};\n    for(int r=0;r<rows;++r)for(int c=0;c<cols;++c)if(grid[r][c]=='1') {\n        ++count;grid[r][c]='0';vector<pair<int,int>> stack{{r,c}};\n        while(!stack.empty()) {\n            auto [y,x]=stack.back();stack.pop_back();\n            for(int i=0;i<4;++i){int ny=y+dy[i],nx=x+dx[i];\n                if(ny>=0&&ny<rows&&nx>=0&&nx<cols&&grid[ny][nx]=='1'){grid[ny][nx]='0';stack.push_back({ny,nx});}}\n        }\n    }\n    return count;\n}"
    },
    "Bellman-Ford Shortest Paths": {
      "python": "def bellman_ford(n, edges, source):\n    # Directed (from, to, weight) edges; None means unreachable.\n    if n <= 0 or not 0 <= source < n:\n        raise ValueError(\"Invalid vertex count or source\")\n    if any(not (0 <= u < n and 0 <= v < n) for u, v, _ in edges):\n        raise ValueError(\"Invalid edge endpoint\")\n    distance = [None] * n\n    distance[source] = 0\n    for step in range(n):\n        changed = False\n        for u, v, weight in edges:\n            if distance[u] is None:\n                continue\n            candidate = distance[u] + weight\n            if distance[v] is None or candidate < distance[v]:\n                if step == n - 1:\n                    raise ValueError(\"Reachable negative cycle\")\n                distance[v] = candidate\n                changed = True\n        if not changed:\n            break\n    return distance",
      "cpp": "// Directed edges; nullopt means unreachable. Throws for a reachable negative cycle.\nvector<optional<long long>> bellmanFord(int n, const vector<tuple<int,int,int>>& edges, int source) {\n    if (n <= 0 || source < 0 || source >= n) throw invalid_argument(\"Invalid source\");\n    for (auto [u,v,w] : edges)\n        if (u < 0 || u >= n || v < 0 || v >= n) throw invalid_argument(\"Invalid endpoint\");\n    vector<optional<long long>> distance(n);\n    distance[source] = 0;\n    for (int step = 0; step < n; ++step) {\n        bool changed = false;\n        for (auto [u,v,w] : edges) {\n            if (!distance[u]) continue;\n            long long candidate = checkedAdd(*distance[u], w);\n            if (!distance[v] || candidate < *distance[v]) {\n                if (step == n-1) throw invalid_argument(\"Reachable negative cycle\");\n                distance[v] = candidate;\n                changed = true;\n            }\n        }\n        if (!changed) break;\n    }\n    return distance;\n}"
    },
    "Climbing Stairs": {
      "python": "def climbing_stairs(n):\n    if n < 0:\n        raise ValueError(\"n must be nonnegative\")\n    previous, current = 1, 1\n    for _ in range(n):\n        previous, current = current, previous + current\n    return previous",
      "cpp": "long long climbingStairs(int n) {\n    if (n < 0) throw invalid_argument(\"n must be nonnegative\");\n    long long previous = 1, current = 1;\n    for (int i = 1; i < n; ++i) {\n        long long next = checkedAdd(previous, current);\n        previous = current; current = next;\n    }\n    return current;\n}"
    },
    "Edit Distance": {
      "python": "def edit_distance(a, b):\n    # Unit-cost insert, delete, replace. Python compares Unicode code points.\n    if len(a) < len(b):\n        a, b = b, a\n    previous = list(range(len(b) + 1))\n    for i, x in enumerate(a, 1):\n        current = [i] + [0] * len(b)\n        for j, y in enumerate(b, 1):\n            current[j] = previous[j-1] if x == y else 1 + min(\n                previous[j], current[j-1], previous[j-1])\n        previous = current\n    return previous[-1]",
      "cpp": "// Compares bytes, not Unicode graphemes. O(m*n) time, O(min(m,n)) space.\nint editDistance(string a, string b) {\n    if (a.size() < b.size()) swap(a,b);\n    vector<int> previous(b.size()+1);\n    iota(previous.begin(), previous.end(), 0);\n    for (int i=1; i<=static_cast<int>(a.size()); ++i) {\n        vector<int> current(b.size()+1); current[0]=i;\n        for (int j=1; j<=static_cast<int>(b.size()); ++j)\n            current[j] = a[i-1]==b[j-1] ? previous[j-1] :\n                1+min({previous[j],current[j-1],previous[j-1]});\n        previous.swap(current);\n    }\n    return previous.back();\n}"
    },
    "Search Rotated Sorted Array": {
      "python": "def search_rotated(a, target):\n    # Strictly increasing array rotated at most once; no duplicates.\n    left, right = 0, len(a) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if a[mid] == target:\n            return mid\n        if a[left] <= a[mid]:\n            if a[left] <= target < a[mid]:\n                right = mid - 1\n            else:\n                left = mid + 1\n        elif a[mid] < target <= a[right]:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1",
      "cpp": "// Preconditions: distinct values, ascending order rotated at most once.\nint searchRotated(const vector<int>& a, int target) {\n    int left=0, right=static_cast<int>(a.size())-1;\n    while (left<=right) {\n        int mid=left+(right-left)/2;\n        if (a[mid]==target) return mid;\n        if (a[left]<=a[mid]) {\n            if (a[left]<=target && target<a[mid]) right=mid-1;\n            else left=mid+1;\n        } else if (a[mid]<target && target<=a[right]) left=mid+1;\n        else right=mid-1;\n    }\n    return -1;\n}"
    },
    "Integer Square Root": {
      "python": "def integer_sqrt(x):\n    if x < 0:\n        raise ValueError(\"x must be nonnegative\")\n    left, right = 0, x\n    while left <= right:\n        mid = (left + right) // 2\n        if mid * mid <= x:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return right",
      "cpp": "int integerSqrt(int x) {\n    if (x<0) throw invalid_argument(\"x must be nonnegative\");\n    long long left=0, right=x;\n    while (left<=right) {\n        long long mid=left+(right-left)/2;\n        if (mid*mid<=x) left=mid+1;\n        else right=mid-1;\n    }\n    return static_cast<int>(right);\n}"
    },
    "Search Row and Column Sorted Matrix": {
      "python": "def search_matrix(matrix, target):\n    # Each row and each column is ascending. Does not require flattened order.\n    if not matrix:\n        return False\n    cols = len(matrix[0])\n    if any(len(row) != cols for row in matrix):\n        raise ValueError(\"Matrix must be rectangular\")\n    row, col = 0, cols - 1\n    while row < len(matrix) and col >= 0:\n        value = matrix[row][col]\n        if value == target:\n            return True\n        if value > target:\n            col -= 1\n        else:\n            row += 1\n    return False",
      "cpp": "bool searchMatrix(const vector<vector<int>>& matrix, int target) {\n    if (matrix.empty()) return false;\n    int cols=static_cast<int>(matrix[0].size());\n    for (const auto& row:matrix)\n        if (row.size()!=static_cast<size_t>(cols)) throw invalid_argument(\"Ragged matrix\");\n    int row=0, col=cols-1;\n    while (row<static_cast<int>(matrix.size()) && col>=0) {\n        if (matrix[row][col]==target) return true;\n        if (matrix[row][col]>target) --col; else ++row;\n    }\n    return false;\n}"
    },
    "Move Zeroes": {
      "python": "def move_zeroes(a):\n    # Stable, in-place compaction; returns the same list for convenience.\n    write = 0\n    for value in a:\n        if value != 0:\n            a[write] = value\n            write += 1\n    for i in range(write, len(a)):\n        a[i] = 0\n    return a",
      "cpp": "void moveZeroes(vector<int>& a) {\n    size_t write=0;\n    for (int value:a) if (value!=0) a[write++]=value;\n    fill(a.begin()+write,a.end(),0);\n}"
    },
    "Remove Duplicates from Sorted Array": {
      "python": "def remove_duplicates(a):\n    # Ascending input. Only a[:returned_length] is meaningful afterward.\n    write = 0\n    for value in a:\n        if write == 0 or value != a[write-1]:\n            a[write] = value\n            write += 1\n    return write",
      "cpp": "size_t removeDuplicates(vector<int>& a) {\n    size_t write=0;\n    for (int value:a)\n        if (write==0 || value!=a[write-1]) a[write++]=value;\n    return write;\n}"
    },
    "Dutch National Flag Partition": {
      "python": "def dutch_flag(a):\n    if any(value not in (0, 1, 2) for value in a):\n        raise ValueError(\"Only 0, 1, and 2 are allowed\")\n    low = mid = 0\n    high = len(a) - 1\n    while mid <= high:\n        if a[mid] == 0:\n            a[low], a[mid] = a[mid], a[low]\n            low += 1\n            mid += 1\n        elif a[mid] == 1:\n            mid += 1\n        else:\n            a[mid], a[high] = a[high], a[mid]\n            high -= 1\n    return a",
      "cpp": "void dutchFlag(vector<int>& a) {\n    for (int value:a) if (value<0 || value>2) throw invalid_argument(\"Only 0,1,2 allowed\");\n    int low=0,mid=0,high=static_cast<int>(a.size())-1;\n    while (mid<=high) {\n        if (a[mid]==0) swap(a[low++],a[mid++]);\n        else if (a[mid]==1) ++mid;\n        else swap(a[mid],a[high--]);\n    }\n}"
    },
    "Boyer–Moore Majority Vote": {
      "python": "def majority_element(a):\n    candidate, votes = None, 0\n    for value in a:\n        if votes == 0:\n            candidate = value\n        votes += 1 if value == candidate else -1\n    return candidate if a and sum(x == candidate for x in a) > len(a)//2 else None",
      "cpp": "optional<int> majorityElement(const vector<int>& a) {\n    int candidate=0,votes=0;\n    for (int value:a) {\n        if (votes==0) candidate=value;\n        votes += value==candidate ? 1 : -1;\n    }\n    if (count(a.begin(),a.end(),candidate)>static_cast<ptrdiff_t>(a.size()/2))\n        return candidate;\n    return nullopt;\n}"
    },
    "Maximum Product Subarray": {
      "python": "def max_product(a):\n    if not a:\n        raise ValueError(\"Array must be nonempty\")\n    low = high = best = a[0]\n    for value in a[1:]:\n        low, high = min(value, low*value, high*value), max(value, low*value, high*value)\n        best = max(best, high)\n    return best",
      "cpp": "// Throws if an intermediate product exceeds signed 64-bit range.\nlong long maxProduct(const vector<int>& a) {\n    if (a.empty()) throw invalid_argument(\"Array must be nonempty\");\n    long long low=a[0],high=a[0],best=a[0];\n    for (size_t i=1;i<a.size();++i) {\n        long long x=a[i],p=checkedMultiply(low,x),q=checkedMultiply(high,x);\n        low=min({x,p,q}); high=max({x,p,q}); best=max(best,high);\n    }\n    return best;\n}"
    },
    "Minimum Length Positive-Sum Window": {
      "python": "def min_window_length(a, target):\n    # Nonnegative values, positive target; 0 means no qualifying window.\n    if target <= 0 or any(x < 0 for x in a):\n        raise ValueError(\"Require target > 0 and values >= 0\")\n    left = total = 0\n    best = len(a) + 1\n    for right, value in enumerate(a):\n        total += value\n        while total >= target:\n            best = min(best, right-left+1)\n            total -= a[left]\n            left += 1\n    return 0 if best > len(a) else best",
      "cpp": "int minWindowLength(const vector<int>& a, long long target) {\n    if (target<=0 || any_of(a.begin(),a.end(),[](int x){return x<0;}))\n        throw invalid_argument(\"Require positive target and nonnegative values\");\n    size_t left=0,best=a.size()+1;\n    long long total=0;\n    for (size_t right=0;right<a.size();++right) {\n        total=checkedAdd(total,a[right]);\n        while (total>=target) {\n            best=min(best,right-left+1);\n            total-=a[left++];\n        }\n    }\n    return best>a.size() ? 0 : static_cast<int>(best);\n}"
    },
    "Floyd–Warshall All-Pairs Shortest Paths": {
      "python": "def floyd_warshall(matrix):\n    # None means no edge; input is copied. Raises on any negative cycle.\n    n = len(matrix)\n    if any(len(row) != n for row in matrix):\n        raise ValueError(\"Matrix must be square\")\n    distance = [row[:] for row in matrix]\n    for i in range(n):\n        distance[i][i] = min(0, distance[i][i]) if distance[i][i] is not None else 0\n    for k in range(n):\n        for i in range(n):\n            if distance[i][k] is None:\n                continue\n            for j in range(n):\n                if distance[k][j] is not None:\n                    candidate = distance[i][k] + distance[k][j]\n                    if distance[i][j] is None or candidate < distance[i][j]:\n                        distance[i][j] = candidate\n    if any(distance[i][i] < 0 for i in range(n)):\n        raise ValueError(\"Negative cycle\")\n    return distance",
      "cpp": "// nullopt = no edge. Throws on any negative cycle or arithmetic overflow.\nvector<vector<optional<long long>>> floydWarshall(vector<vector<optional<long long>>> d) {\n    size_t n=d.size();\n    for (const auto& row:d) if (row.size()!=n) throw invalid_argument(\"Matrix must be square\");\n    for (size_t i=0;i<n;++i) d[i][i]=d[i][i] ? min(0LL,*d[i][i]) : 0;\n    for (size_t k=0;k<n;++k)\n        for (size_t i=0;i<n;++i) if (d[i][k])\n            for (size_t j=0;j<n;++j) if (d[k][j]) {\n                long long candidate=checkedAdd(*d[i][k],*d[k][j]);\n                if (!d[i][j] || candidate<*d[i][j]) d[i][j]=candidate;\n            }\n    for (size_t i=0;i<n;++i) if (*d[i][i]<0) throw invalid_argument(\"Negative cycle\");\n    return d;\n}"
    },
    "Bipartite Graph Check": {
      "python": "from collections import deque\n\ndef is_bipartite(graph):\n    # Undirected adjacency list with vertex indices in [0, len(graph)).\n    color = [-1] * len(graph)\n    for start in range(len(graph)):\n        if color[start] != -1:\n            continue\n        color[start] = 0\n        queue = deque([start])\n        while queue:\n            u = queue.popleft()\n            for v in graph[u]:\n                if not 0 <= v < len(graph):\n                    raise ValueError(\"Invalid vertex\")\n                if color[v] == -1:\n                    color[v] = 1 - color[u]\n                    queue.append(v)\n                elif color[v] == color[u]:\n                    return False\n    return True",
      "cpp": "// Undirected adjacency list with valid vertex indices.\nbool isBipartite(const vector<vector<int>>& graph) {\n    vector<int> color(graph.size(),-1);\n    for (int start=0;start<static_cast<int>(graph.size());++start) {\n        if (color[start]!=-1) continue;\n        color[start]=0; queue<int> q; q.push(start);\n        while (!q.empty()) {\n            int u=q.front(); q.pop();\n            for (int v:graph[u]) {\n                if (v<0 || v>=static_cast<int>(graph.size())) throw invalid_argument(\"Invalid vertex\");\n                if (color[v]==-1) {color[v]=1-color[u];q.push(v);}\n                else if (color[v]==color[u]) return false;\n            }\n        }\n    }\n    return true;\n}"
    },
    "Count Undirected Connected Components": {
      "python": "def count_components(graph):\n    # Undirected adjacency list, including isolated vertices.\n    seen = set()\n    count = 0\n    for start in range(len(graph)):\n        if start in seen:\n            continue\n        count += 1\n        seen.add(start)\n        stack = [start]\n        while stack:\n            u = stack.pop()\n            for v in graph[u]:\n                if not 0 <= v < len(graph):\n                    raise ValueError(\"Invalid vertex\")\n                if v not in seen:\n                    seen.add(v)\n                    stack.append(v)\n    return count",
      "cpp": "int countComponents(const vector<vector<int>>& graph) {\n    vector<bool> seen(graph.size());\n    int count=0;\n    for (int start=0;start<static_cast<int>(graph.size());++start) {\n        if (seen[start]) continue;\n        ++count; seen[start]=true; vector<int> stack{start};\n        while (!stack.empty()) {\n            int u=stack.back(); stack.pop_back();\n            for (int v:graph[u]) {\n                if (v<0 || v>=static_cast<int>(graph.size())) throw invalid_argument(\"Invalid vertex\");\n                if (!seen[v]) {seen[v]=true;stack.push_back(v);}\n            }\n        }\n    }\n    return count;\n}"
    },
    "Prim Minimum Spanning Tree (Dense)": {
      "python": "def prim_mst(matrix):\n    # Symmetric matrix; None = no edge. Supports zero and negative weights.\n    n = len(matrix)\n    if any(len(row) != n for row in matrix):\n        raise ValueError(\"Matrix must be square\")\n    if any(matrix[i][j] != matrix[j][i] for i in range(n) for j in range(n)):\n        raise ValueError(\"Matrix must be symmetric\")\n    if not n:\n        return 0\n    used = [False] * n\n    best = [None] * n\n    best[0] = 0\n    total = 0\n    for _ in range(n):\n        u = -1\n        for v in range(n):\n            if not used[v] and best[v] is not None and (u == -1 or best[v] < best[u]):\n                u = v\n        if u == -1:\n            return None  # Disconnected graph: no spanning tree.\n        used[u] = True\n        total += best[u]\n        for v, weight in enumerate(matrix[u]):\n            if not used[v] and weight is not None and (best[v] is None or weight < best[v]):\n                best[v] = weight\n    return total",
      "cpp": "optional<long long> primMst(const vector<vector<optional<int>>>& matrix) {\n    size_t n=matrix.size();\n    for (const auto& row:matrix) if (row.size()!=n) throw invalid_argument(\"Matrix must be square\");\n    for (size_t i=0;i<n;++i) for (size_t j=0;j<n;++j)\n        if (matrix[i][j]!=matrix[j][i]) throw invalid_argument(\"Matrix must be symmetric\");\n    if (n==0) return 0;\n    vector<bool> used(n);\n    vector<optional<int>> best(n); best[0]=0;\n    long long total=0;\n    for (size_t step=0;step<n;++step) {\n        int u=-1;\n        for (int v=0;v<static_cast<int>(n);++v)\n            if (!used[v] && best[v] && (u==-1 || *best[v]<*best[u])) u=v;\n        if (u==-1) return nullopt;\n        used[u]=true; total=checkedAdd(total,*best[u]);\n        for (size_t v=0;v<n;++v)\n            if (!used[v] && matrix[u][v] && (!best[v] || *matrix[u][v]<*best[v]))\n                best[v]=matrix[u][v];\n    }\n    return total;\n}"
    },
    "Multi-Source BFS Distances": {
      "python": "from collections import deque\n\ndef multi_source_bfs(graph, sources):\n    # Unweighted graph; -1 = unreachable. Duplicate sources are ignored.\n    distance = [-1] * len(graph)\n    queue = deque()\n    for source in sources:\n        if not 0 <= source < len(graph):\n            raise ValueError(\"Invalid source\")\n        if distance[source] == -1:\n            distance[source] = 0\n            queue.append(source)\n    while queue:\n        u = queue.popleft()\n        for v in graph[u]:\n            if not 0 <= v < len(graph):\n                raise ValueError(\"Invalid vertex\")\n            if distance[v] == -1:\n                distance[v] = distance[u] + 1\n                queue.append(v)\n    return distance",
      "cpp": "vector<int> multiSourceBfs(const vector<vector<int>>& graph, const vector<int>& sources) {\n    vector<int> distance(graph.size(),-1); queue<int> q;\n    for (int source:sources) {\n        if (source<0 || source>=static_cast<int>(graph.size())) throw invalid_argument(\"Invalid source\");\n        if (distance[source]==-1) {distance[source]=0;q.push(source);}\n    }\n    while (!q.empty()) {\n        int u=q.front(); q.pop();\n        for (int v:graph[u]) {\n            if (v<0 || v>=static_cast<int>(graph.size())) throw invalid_argument(\"Invalid vertex\");\n            if (distance[v]==-1) {distance[v]=distance[u]+1;q.push(v);}\n        }\n    }\n    return distance;\n}"
    },
    "Euclidean Greatest Common Divisor": {
      "python": "def gcd(a, b):\n    a, b = abs(a), abs(b)\n    while b:\n        a, b = b, a % b\n    return a  # gcd(0,0) is defined here as 0.",
      "cpp": "// int inputs; long long safely represents abs(INT_MIN).\nlong long gcd(int a, int b) {\n    long long x=a,y=b;\n    if (x<0) x=-x;\n    if (y<0) y=-y;\n    while (y!=0) {long long remainder=x%y;x=y;y=remainder;}\n    return x;\n}"
    },
    "Sieve of Eratosthenes": {
      "python": "def sieve(n):\n    if n < 0:\n        raise ValueError(\"n must be nonnegative\")\n    prime = [True] * (n + 1)\n    for i in range(min(2, n+1)):\n        prime[i] = False\n    p = 2\n    while p*p <= n:\n        if prime[p]:\n            for multiple in range(p*p, n+1, p):\n                prime[multiple] = False\n        p += 1\n    return [i for i, is_prime in enumerate(prime) if is_prime]",
      "cpp": "vector<int> sieve(int n) {\n    if (n<0) throw invalid_argument(\"n must be nonnegative\");\n    vector<bool> prime(static_cast<size_t>(n)+1,true);\n    prime[0]=false; if (n>=1) prime[1]=false;\n    for (long long p=2;p*p<=n;++p)\n        if (prime[p]) for (long long multiple=p*p;multiple<=n;multiple+=p) prime[multiple]=false;\n    vector<int> result;\n    for (long long i=2;i<=n;++i) if (prime[i]) result.push_back(static_cast<int>(i));\n    return result;\n}"
    },
    "Modular Exponentiation by Squaring": {
      "python": "def mod_power(base, exponent, modulus):\n    if exponent < 0 or modulus <= 0:\n        raise ValueError(\"Require exponent >= 0 and modulus > 0\")\n    base %= modulus\n    result = 1 % modulus\n    while exponent:\n        if exponent & 1:\n            result = result * base % modulus\n        base = base * base % modulus\n        exponent >>= 1\n    return result",
      "cpp": "// Positive int modulus keeps reduced-residue products within long long.\nlong long modPower(long long base, long long exponent, int modulus) {\n    if (exponent<0 || modulus<=0) throw invalid_argument(\"Invalid exponent or modulus\");\n    base%=modulus; if (base<0) base+=modulus;\n    long long result=1%modulus;\n    while (exponent) {\n        if (exponent&1) result=result*base%modulus;\n        base=base*base%modulus; exponent>>=1;\n    }\n    return result;\n}"
    },
    "Count Set Bits (Kernighan)": {
      "python": "def count_set_bits(n):\n    # Explicit 32-bit unsigned domain, matching the C++ example.\n    if not 0 <= n <= 0xFFFFFFFF:\n        raise ValueError(\"Require a 32-bit unsigned integer\")\n    count = 0\n    while n:\n        n &= n - 1\n        count += 1\n    return count",
      "cpp": "int countSetBits(uint32_t n) {\n    int count=0;\n    while (n) {n &= n-1; ++count;}\n    return count;\n}"
    },
    "Unique Grid Paths": {
      "python": "def unique_paths(rows, cols):\n    if rows < 0 or cols < 0:\n        raise ValueError(\"Dimensions must be nonnegative\")\n    if not rows or not cols:\n        return 0\n    rows, cols = max(rows, cols), min(rows, cols)\n    ways = [1] * cols\n    for _ in range(1, rows):\n        for c in range(1, cols):\n            ways[c] += ways[c-1]\n    return ways[-1]",
      "cpp": "long long uniquePaths(int rows, int cols) {\n    if (rows<0 || cols<0) throw invalid_argument(\"Negative dimension\");\n    if (rows==0 || cols==0) return 0;\n    if (rows<cols) swap(rows,cols);\n    vector<long long> ways(cols,1);\n    for (int r=1;r<rows;++r)\n        for (int c=1;c<cols;++c) ways[c]=checkedAdd(ways[c],ways[c-1]);\n    return ways.back();\n}"
    },
    "Minimum Grid Path Sum": {
      "python": "def min_path_sum(grid):\n    # Right/down moves only. Negative costs are allowed; input is not modified.\n    if not grid:\n        return 0\n    cols = len(grid[0])\n    if any(len(row) != cols for row in grid):\n        raise ValueError(\"Grid must be rectangular\")\n    if not cols:\n        return 0\n    cost = [0] * cols\n    for r, row in enumerate(grid):\n        for c, value in enumerate(row):\n            if r == 0 and c == 0:\n                cost[c] = value\n            elif r == 0:\n                cost[c] = cost[c-1] + value\n            elif c == 0:\n                cost[c] += value\n            else:\n                cost[c] = min(cost[c], cost[c-1]) + value\n    return cost[-1]",
      "cpp": "long long minPathSum(const vector<vector<int>>& grid) {\n    if (grid.empty()) return 0;\n    size_t cols=grid[0].size();\n    for (const auto& row:grid) if (row.size()!=cols) throw invalid_argument(\"Ragged grid\");\n    if (cols==0) return 0;\n    vector<long long> cost(cols);\n    for (size_t r=0;r<grid.size();++r)\n        for (size_t c=0;c<cols;++c) {\n            if (r==0 && c==0) cost[c]=grid[r][c];\n            else if (r==0) cost[c]=checkedAdd(cost[c-1],grid[r][c]);\n            else if (c==0) cost[c]=checkedAdd(cost[c],grid[r][c]);\n            else cost[c]=checkedAdd(min(cost[c],cost[c-1]),grid[r][c]);\n        }\n    return cost.back();\n}"
    },
    "Partition Equal Subset Sum": {
      "python": "def can_partition(a):\n    if any(x < 0 for x in a):\n        raise ValueError(\"Values must be nonnegative\")\n    total = sum(a)\n    if total % 2:\n        return False\n    target = total // 2\n    possible = [False] * (target + 1)\n    possible[0] = True\n    for value in a:\n        for amount in range(target, value-1, -1):\n            possible[amount] |= possible[amount-value]\n    return possible[target]",
      "cpp": "// Pseudo-polynomial: O(n*sum) time, O(sum) space.\nbool canPartition(const vector<int>& a) {\n    long long total=0;\n    for (int x:a) {\n        if (x<0) throw invalid_argument(\"Values must be nonnegative\");\n        total=checkedAdd(total,x);\n    }\n    if (total%2) return false;\n    long long target=total/2;\n    // Capacity is explicitly limited to the integer-indexed DP domain.\n    if (target>=numeric_limits<int>::max()) throw length_error(\"DP target is too large\");\n    vector<bool> possible(static_cast<size_t>(target)+1);\n    possible[0]=true;\n    for (int value:a)\n        for (int amount=static_cast<int>(target);amount>=value;--amount)\n            possible[amount]=possible[amount] || possible[amount-value];\n    return possible[target];\n}"
    },
    "Word Break": {
      "python": "def word_break(text, words):\n    # Substring creation makes worst-case character work O(n^3).\n    words = set(words)\n    longest = max(map(len, words), default=0)\n    possible = [True] + [False] * len(text)\n    for end in range(1, len(text)+1):\n        possible[end] = any(possible[start] and text[start:end] in words\n                            for start in range(max(0, end-longest), end))\n    return possible[-1]",
      "cpp": "bool wordBreak(const string& text, const vector<string>& words) {\n    unordered_set<string> dictionary(words.begin(),words.end());\n    size_t longest=0;\n    for (const auto& word:words) longest=max(longest,word.size());\n    vector<bool> possible(text.size()+1); possible[0]=true;\n    for (size_t end=1;end<=text.size();++end)\n        for (size_t start=end>longest ? end-longest : 0;start<end;++start)\n            if (possible[start] && dictionary.count(text.substr(start,end-start))) {\n                possible[end]=true; break;\n            }\n    return possible.back();\n}"
    },
    "Decode Ways": {
      "python": "def decode_ways(text):\n    # Mapping 1..26 -> A..Z; zero cannot stand alone.\n    if any(c < \"0\" or c > \"9\" for c in text):\n        raise ValueError(\"ASCII digits required\")\n    if not text:\n        return 0\n    previous, current = 1, int(text[0] != \"0\")\n    for i in range(1, len(text)):\n        next_count = current if text[i] != \"0\" else 0\n        if 10 <= int(text[i-1:i+1]) <= 26:\n            next_count += previous\n        previous, current = current, next_count\n    return current",
      "cpp": "long long decodeWays(const string& text) {\n    for (char c:text) if (c<'0' || c>'9') throw invalid_argument(\"ASCII digits required\");\n    if (text.empty()) return 0;\n    long long previous=1,current=text[0]!='0';\n    for (size_t i=1;i<text.size();++i) {\n        long long next=text[i]!='0' ? current : 0;\n        int pair=(text[i-1]-'0')*10+text[i]-'0';\n        if (pair>=10 && pair<=26) next=checkedAdd(next,previous);\n        previous=current; current=next;\n    }\n    return current;\n}"
    },
    "Longest Palindromic Substring": {
      "python": "def longest_palindrome(text):\n    # Expand odd/even centers; first longest palindrome wins ties.\n    start = length = 0\n    for center in range(len(text)):\n        for left, right in ((center, center), (center, center+1)):\n            while left >= 0 and right < len(text) and text[left] == text[right]:\n                if right-left+1 > length:\n                    start, length = left, right-left+1\n                left -= 1\n                right += 1\n    return text[start:start+length]",
      "cpp": "// Byte strings. O(n^2) time and O(1) auxiliary space excluding output.\nstring longestPalindrome(const string& text) {\n    int start=0,length=0,n=static_cast<int>(text.size());\n    for (int center=0;center<n;++center)\n        for (int even=0;even<=1;++even) {\n            int left=center,right=center+even;\n            while (left>=0 && right<n && text[left]==text[right]) {\n                if (right-left+1>length) {start=left;length=right-left+1;}\n                --left; ++right;\n            }\n        }\n    return text.substr(start,length);\n}"
    },
    "Z Algorithm": {
      "python": "def z_array(text):\n    # z[i] = length of prefix matching text[i:]; convention z[0] = 0.\n    z = [0] * len(text)\n    left = right = 0  # Current matching interval [left, right).\n    for i in range(1, len(text)):\n        if i < right:\n            z[i] = min(right-i, z[i-left])\n        while i+z[i] < len(text) and text[z[i]] == text[i+z[i]]:\n            z[i] += 1\n        if i+z[i] > right:\n            left, right = i, i+z[i]\n    return z",
      "cpp": "vector<int> zArray(const string& text) {\n    int n=static_cast<int>(text.size()),left=0,right=0;\n    vector<int> z(n);\n    for (int i=1;i<n;++i) {\n        if (i<right) z[i]=min(right-i,z[i-left]);\n        while (i+z[i]<n && text[z[i]]==text[i+z[i]]) ++z[i];\n        if (i+z[i]>right) {left=i;right=i+z[i];}\n    }\n    return z;\n}"
    },
    "Find All Anagram Windows": {
      "python": "def find_anagrams(text, pattern):\n    if any(c < \"a\" or c > \"z\" for c in text + pattern):\n        raise ValueError(\"Lowercase ASCII letters required\")\n    if not pattern or len(pattern) > len(text):\n        return []\n    need, window = [0]*26, [0]*26\n    for c in pattern:\n        need[ord(c)-97] += 1\n    result = []\n    for right, c in enumerate(text):\n        window[ord(c)-97] += 1\n        if right >= len(pattern):\n            window[ord(text[right-len(pattern)])-97] -= 1\n        if right+1 >= len(pattern) and window == need:\n            result.append(right-len(pattern)+1)\n    return result",
      "cpp": "vector<int> findAnagrams(const string& text, const string& pattern) {\n    for (const auto* value:{&text,&pattern})\n        for (char c:*value) if (c<'a' || c>'z') throw invalid_argument(\"Lowercase ASCII required\");\n    if (pattern.empty() || pattern.size()>text.size()) return {};\n    array<int,26> need{},window{};\n    for (char c:pattern) ++need[c-'a'];\n    vector<int> result;\n    for (size_t right=0;right<text.size();++right) {\n        ++window[text[right]-'a'];\n        if (right>=pattern.size()) --window[text[right-pattern.size()]-'a'];\n        if (right+1>=pattern.size() && window==need)\n            result.push_back(static_cast<int>(right-pattern.size()+1));\n    }\n    return result;\n}"
    },
    "Run-Length Encoding": {
      "python": "def run_length_encode(text):\n    # Structured pairs avoid ambiguity when the text contains digits.\n    result = []\n    for char in text:\n        if result and result[-1][0] == char:\n            previous, count = result[-1]\n            result[-1] = (previous, count+1)\n        else:\n            result.append((char, 1))\n    return result",
      "cpp": "// Byte-oriented encoding. Structured pairs remain unambiguous for digit input.\nvector<pair<char,size_t>> runLengthEncode(const string& text) {\n    vector<pair<char,size_t>> result;\n    for (char c:text) {\n        if (!result.empty() && result.back().first==c) ++result.back().second;\n        else result.emplace_back(c,1);\n    }\n    return result;\n}"
    },
    "Merge K Sorted Arrays": {
      "python": "import heapq\n\ndef merge_k_arrays(arrays):\n    # Every input array must already be sorted ascending.\n    heap = [(row[0], r, 0) for r, row in enumerate(arrays) if row]\n    heapq.heapify(heap)\n    result = []\n    while heap:\n        value, row, index = heapq.heappop(heap)\n        result.append(value)\n        if index+1 < len(arrays[row]):\n            heapq.heappush(heap, (arrays[row][index+1], row, index+1))\n    return result",
      "cpp": "vector<int> mergeKArrays(const vector<vector<int>>& arrays) {\n    using Item=tuple<int,size_t,size_t>;\n    priority_queue<Item,vector<Item>,greater<Item>> heap;\n    for (size_t r=0;r<arrays.size();++r)\n        if (!arrays[r].empty()) heap.emplace(arrays[r][0],r,0);\n    vector<int> result;\n    while (!heap.empty()) {\n        auto [value,row,index]=heap.top(); heap.pop();\n        result.push_back(value);\n        if (index+1<arrays[row].size()) heap.emplace(arrays[row][index+1],row,index+1);\n    }\n    return result;\n}"
    },
    "Maximum Nonoverlapping Intervals": {
      "python": "def max_nonoverlapping(intervals):\n    # Half-open [start,end) intervals; touching endpoints do not overlap.\n    if any(start >= end for start, end in intervals):\n        raise ValueError(\"Every interval must have start < end\")\n    previous_end = None\n    count = 0\n    for start, end in sorted(intervals, key=lambda item: (item[1], item[0])):\n        if previous_end is None or start >= previous_end:\n            previous_end = end\n            count += 1\n    return count",
      "cpp": "int maxNonoverlapping(vector<pair<int,int>> intervals) {\n    for (auto [start,end]:intervals)\n        if (start>=end) throw invalid_argument(\"Require start < end\");\n    sort(intervals.begin(),intervals.end(),[](const auto& a,const auto& b) {\n        return a.second!=b.second ? a.second<b.second : a.first<b.first;\n    });\n    optional<int> previousEnd; int count=0;\n    for (auto [start,end]:intervals)\n        if (!previousEnd || start>=*previousEnd) {previousEnd=end;++count;}\n    return count;\n}"
    },
    "Minimum Meeting Rooms": {
      "python": "import heapq\n\ndef meeting_rooms(intervals):\n    # Half-open [start,end); a room can be reused at exactly its end time.\n    if any(start >= end for start, end in intervals):\n        raise ValueError(\"Every interval must have start < end\")\n    occupied = []\n    best = 0\n    for start, end in sorted(intervals):\n        while occupied and occupied[0] <= start:\n            heapq.heappop(occupied)\n        heapq.heappush(occupied, end)\n        best = max(best, len(occupied))\n    return best",
      "cpp": "int meetingRooms(vector<pair<int,int>> intervals) {\n    for (auto [start,end]:intervals)\n        if (start>=end) throw invalid_argument(\"Require start < end\");\n    sort(intervals.begin(),intervals.end());\n    priority_queue<int,vector<int>,greater<int>> occupied;\n    size_t best=0;\n    for (auto [start,end]:intervals) {\n        while (!occupied.empty() && occupied.top()<=start) occupied.pop();\n        occupied.push(end); best=max(best,occupied.size());\n    }\n    return static_cast<int>(best);\n}"
    },
    "Minimum Jumps to Reach End": {
      "python": "def min_jumps(a):\n    if any(x < 0 for x in a):\n        raise ValueError(\"Jump lengths must be nonnegative\")\n    if not a:\n        return -1\n    jumps = end = farthest = 0\n    for i in range(len(a)-1):\n        if i > farthest:\n            return -1\n        farthest = max(farthest, i+a[i])\n        if i == end:\n            if farthest == end:\n                return -1\n            jumps += 1\n            end = farthest\n            if end >= len(a)-1:\n                return jumps\n    return jumps if end >= len(a)-1 else -1",
      "cpp": "int minJumps(const vector<int>& a) {\n    for (int value:a) if (value<0) throw invalid_argument(\"Negative jump length\");\n    if (a.empty()) return -1;\n    long long end=0,farthest=0; int jumps=0;\n    for (long long i=0;i<static_cast<long long>(a.size())-1;++i) {\n        if (i>farthest) return -1;\n        farthest=max(farthest,i+a[i]);\n        if (i==end) {\n            if (farthest==end) return -1;\n            ++jumps; end=farthest;\n            if (end>=static_cast<long long>(a.size())-1) return jumps;\n        }\n    }\n    return end>=static_cast<long long>(a.size())-1 ? jumps : -1;\n}"
    },
    "Generate Balanced Parentheses": {
      "python": "def generate_parentheses(n):\n    if n < 0:\n        raise ValueError(\"n must be nonnegative\")\n    result, path = [], []\n    def visit(opened, closed):\n        if closed == n:\n            result.append(\"\".join(path))\n            return\n        if opened < n:\n            path.append(\"(\")\n            visit(opened+1, closed)\n            path.pop()\n        if closed < opened:\n            path.append(\")\")\n            visit(opened, closed+1)\n            path.pop()\n    visit(0, 0)\n    return result",
      "cpp": "vector<string> generateParentheses(int n) {\n    if (n<0) throw invalid_argument(\"n must be nonnegative\");\n    vector<string> result; string path;\n    function<void(int,int)> visit=[&](int opened,int closed) {\n        if (closed==n) {result.push_back(path);return;}\n        if (opened<n) {path.push_back('(');visit(opened+1,closed);path.pop_back();}\n        if (closed<opened) {path.push_back(')');visit(opened,closed+1);path.pop_back();}\n    };\n    visit(0,0); return result;\n}"
    },
    "Combination Sum (Reusable Candidates)": {
      "python": "def combination_sum(candidates, target):\n    if target < 0 or any(x <= 0 for x in candidates):\n        raise ValueError(\"Require target >= 0 and positive candidates\")\n    candidates = sorted(set(candidates))\n    result, path = [], []\n    def visit(start, remaining):\n        if remaining == 0:\n            result.append(path.copy())\n            return\n        for i in range(start, len(candidates)):\n            value = candidates[i]\n            if value > remaining:\n                break\n            path.append(value)\n            visit(i, remaining-value)  # Reuse the same candidate.\n            path.pop()\n    visit(0, target)\n    return result",
      "cpp": "vector<vector<int>> combinationSum(vector<int> candidates,int target) {\n    if (target<0 || any_of(candidates.begin(),candidates.end(),[](int x){return x<=0;}))\n        throw invalid_argument(\"Require nonnegative target and positive candidates\");\n    sort(candidates.begin(),candidates.end());\n    candidates.erase(unique(candidates.begin(),candidates.end()),candidates.end());\n    vector<vector<int>> result; vector<int> path;\n    function<void(size_t,int)> visit=[&](size_t start,int remaining) {\n        if (remaining==0) {result.push_back(path);return;}\n        for (size_t i=start;i<candidates.size();++i) {\n            int value=candidates[i]; if (value>remaining) break;\n            path.push_back(value);visit(i,remaining-value);path.pop_back();\n        }\n    };\n    visit(0,target); return result;\n}"
    },
    "Generate K-Combinations": {
      "python": "def combinations(n, k):\n    # Choose k distinct values from 1..n, in lexicographic order.\n    if n < 0 or k < 0 or k > n:\n        raise ValueError(\"Require 0 <= k <= n\")\n    result, path = [], []\n    def visit(start):\n        needed = k - len(path)\n        if needed == 0:\n            result.append(path.copy())\n            return\n        for value in range(start, n-needed+2):\n            path.append(value)\n            visit(value+1)\n            path.pop()\n    visit(1)\n    return result",
      "cpp": "vector<vector<int>> combinations(int n,int k) {\n    if (n<0 || k<0 || k>n) throw invalid_argument(\"Require 0 <= k <= n\");\n    vector<vector<int>> result; vector<int> path;\n    function<void(long long)> visit=[&](long long start) {\n        int needed=k-static_cast<int>(path.size());\n        if (needed==0) {result.push_back(path);return;}\n        for (long long value=start;value<=static_cast<long long>(n)-needed+1;++value) {\n            path.push_back(static_cast<int>(value));visit(value+1);path.pop_back();\n        }\n    };\n    visit(1); return result;\n}"
    },
    "Largest Rectangle in Histogram": {
      "python": "def largest_rectangle(heights):\n    if any(h < 0 for h in heights):\n        raise ValueError(\"Heights must be nonnegative\")\n    stack = []\n    best = 0\n    for i in range(len(heights)+1):\n        height = heights[i] if i < len(heights) else 0\n        while stack and heights[stack[-1]] > height:\n            h = heights[stack.pop()]\n            width = i - stack[-1] - 1 if stack else i\n            best = max(best, h*width)\n        if i < len(heights):\n            stack.append(i)\n    return best",
      "cpp": "long long largestRectangle(const vector<int>& heights) {\n    for (int h:heights) if (h<0) throw invalid_argument(\"Negative height\");\n    vector<size_t> stack; long long best=0;\n    for (size_t i=0;i<=heights.size();++i) {\n        int height=i<heights.size() ? heights[i] : 0;\n        while (!stack.empty() && heights[stack.back()]>height) {\n            int h=heights[stack.back()]; stack.pop_back();\n            size_t width=stack.empty() ? i : i-stack.back()-1;\n            best=max(best,checkedMultiply(h,static_cast<long long>(width)));\n        }\n        if (i<heights.size()) stack.push_back(i);\n    }\n    return best;\n}"
    }
  },
  "algorithmPatterns": {
    "Lower Bound": "binary-search",
    "Breadth-First Search": "bfs",
    "Dijkstra's Algorithm": "dijkstra",
    "Two Sum": "hash-map",
    "Sliding Window Maximum": "monotonic-deque",
    "Topological Sort (Kahn)": "topological",
    "Disjoint Set Union": "union-find",
    "Trie (Prefix Tree)": "trie",
    "Longest Substring Without Repeating": "sliding-window",
    "Reverse Linked List": "linked-reversal",
    "Linked List Cycle": "fast-slow",
    "Merge Intervals": "intervals",
    "Subarray Sum Equals K": "prefix-sum",
    "Maximum Depth of Binary Tree": "tree-dfs",
    "House Robber": "dp-1d",
    "Longest Common Subsequence": "dp-2d",
    "Generate Subsets": "backtracking",
    "Kth Largest with a Bounded Heap": "heap",
    "Jump Game Reachability": "greedy",
    "Daily Temperatures": "monotonic-stack",
    "Single Number with XOR": "bitwise"
  }
};
