<UserControl x:Class="AlgorithmVault.Controls.NotesHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackground}" FontFamily="Segoe UI Variable Text, Segoe UI">
    <UserControl.Resources>
        <Style x:Key="NotesField" TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Background" Value="{DynamicResource FieldBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="13,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="9">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers><Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Accent}"/></Trigger></ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NotesButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/><Setter Property="Background" Value="{DynamicResource Accent}"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="14,9"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{DynamicResource Border}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="9" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Opacity" Value="0.84"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="NotesSecondaryButton" TargetType="Button" BasedOn="{StaticResource NotesButton}">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/><Setter Property="Background" Value="{DynamicResource ButtonSurface}"/><Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="MicroLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SectionText}"/><Setter Property="FontSize" Value="9"/><Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="380"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>

        <!-- Thread browser -->
        <Border Grid.Column="0" Background="{DynamicResource LibraryBackground}" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,1,0">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <StackPanel Margin="24,26,24,16">
                    <DockPanel LastChildFill="True">
                        <Border DockPanel.Dock="Right" Background="{DynamicResource AccentSoft}" CornerRadius="10" Padding="10,5" VerticalAlignment="Center"><TextBlock x:Name="ThreadCountText" Text="0" Foreground="{DynamicResource Accent}" FontSize="11" FontWeight="SemiBold"/></Border>
                        <StackPanel><TextBlock Text="NOTES HUB" Foreground="{DynamicResource Mint}" FontSize="10" FontWeight="Bold"/><TextBlock Text="Semester threads" Foreground="{DynamicResource TextPrimary}" FontSize="23" FontWeight="SemiBold" Margin="0,5,12,0"/></StackPanel>
                    </DockPanel>
                    <TextBlock Text="Keep every course, resource, and discussion in one private place." Foreground="{DynamicResource TextSecondary}" FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="24,0,24,16">
                    <Grid>
                        <TextBlock Text="⌕" Foreground="{DynamicResource TextSecondary}" FontSize="18" Margin="12,0,0,0" VerticalAlignment="Center" Panel.ZIndex="2" IsHitTestVisible="False"/>
                        <TextBox x:Name="ThreadSearchBox" Style="{StaticResource NotesField}" Padding="38,10,12,10" TextChanged="Filter_Changed"/>
                        <TextBlock Text="Search threads, courses, notes..." Foreground="{DynamicResource SectionText}" Margin="38,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                            <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Text, ElementName=ThreadSearchBox}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    <ComboBox x:Name="SemesterFilter" Margin="0,9,0,0" Style="{DynamicResource ComboField}" SelectionChanged="Filter_Changed" SelectedIndex="0">
                        <ComboBoxItem Content="All semesters"/><ComboBoxItem Content="Getting Started"/><ComboBoxItem Content="Semester 1"/><ComboBoxItem Content="Semester 2"/><ComboBoxItem Content="Semester 3"/><ComboBoxItem Content="Semester 4"/><ComboBoxItem Content="Semester 5"/><ComboBoxItem Content="Semester 6"/><ComboBoxItem Content="Semester 7"/><ComboBoxItem Content="Semester 8"/><ComboBoxItem Content="Archive"/>
                    </ComboBox>
                </StackPanel>

                <ListBox Grid.Row="2" ItemsSource="{Binding FilteredThreads}" SelectedItem="{Binding SelectedThread, Mode=TwoWay}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimary}" Margin="12,0,12,8" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,2"/><Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="1" CornerRadius="11" Padding="13"><ContentPresenter/></Border><ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="Root" Property="Background" Value="{DynamicResource SelectionBackground}"/><Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentSoft}"/></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Background" Value="{DynamicResource HoverBackground}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <DockPanel LastChildFill="True">
                                    <TextBlock DockPanel.Dock="Right" Text="●" Foreground="#F4C95D" FontSize="8" Margin="9,0,0,0" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsPinned}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
                                    <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextPrimary}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                </DockPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,7,0,0">
                                    <Border Background="#1B3042" CornerRadius="5" Padding="7,3" Margin="0,0,6,0"><TextBlock Text="{Binding Course}" Foreground="#8ED1F4" FontSize="9"/></Border>
                                    <Border Background="{DynamicResource AccentSoft}" CornerRadius="5" Padding="7,3"><TextBlock Text="{Binding Semester}" Foreground="{DynamicResource TextPrimary}" FontSize="9"/></Border>
                                </StackPanel>
                                <TextBlock Text="{Binding Body}" Foreground="{DynamicResource TextSecondary}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,8,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <TextBlock Text="💬" FontSize="10"/><TextBlock Text="{Binding Replies.Count}" Foreground="{DynamicResource SectionText}" FontSize="10" Margin="4,0,12,0"/>
                                    <TextBlock Text="📎" FontSize="10"/><TextBlock Text="{Binding Attachments.Count}" Foreground="{DynamicResource SectionText}" FontSize="10" Margin="4,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Grid.Row="3" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0" Padding="24,14">
                    <Button Content="＋  Create new thread" Click="NewThread_Click" Style="{StaticResource NotesButton}" HorizontalAlignment="Stretch"/>
                </Border>
            </Grid>
        </Border>

        <!-- Thread detail -->
        <Border Grid.Column="1" Background="{DynamicResource DetailBackground}" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid Margin="36,30,36,38" IsEnabled="{Binding HasThread}" MaxWidth="1120" HorizontalAlignment="Stretch">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                            <Button x:Name="PinButton" Content="Pin" Click="Pin_Click" Style="{StaticResource NotesSecondaryButton}" Width="82" Height="42"/>
                            <Button Content="Delete" Click="DeleteThread_Click" Style="{StaticResource NotesSecondaryButton}" Foreground="#EF7181" Margin="8,0,0,0" Width="82" Height="42"/>
                            <Button Content="Save thread" Click="SaveThread_Click" Style="{StaticResource NotesButton}" Margin="8,0,0,0" Width="112" Height="42"/>
                        </StackPanel>
                        <StackPanel Grid.Column="0" Margin="0,0,24,0">
                            <TextBlock Text="THREAD DETAILS" Style="{StaticResource MicroLabel}"/>
                            <TextBox Text="{Binding SelectedThread.Title, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" CaretBrush="{DynamicResource TextPrimary}" FontSize="27" FontWeight="SemiBold" Padding="0" Margin="0,7,20,0"/>
                            <TextBlock x:Name="ThreadMetaText" Text="Choose a thread or create a new one" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="1,3,0,0"/>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,26,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="16"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="COURSE / SUBJECT" Style="{StaticResource MicroLabel}" Margin="1,0,0,7"/><TextBox Text="{Binding SelectedThread.Course, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NotesField}"/></StackPanel>
                        <StackPanel Grid.Column="2"><TextBlock Text="SEMESTER" Style="{StaticResource MicroLabel}" Margin="1,0,0,7"/><ComboBox IsEditable="True" StaysOpenOnEdit="True" Style="{DynamicResource ComboField}" ItemsSource="{Binding SemesterOptions}" Text="{Binding SelectedThread.Semester, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                    </Grid>

                    <StackPanel Grid.Row="2" Margin="0,22,0,0">
                        <TextBlock Text="MAIN NOTE" Style="{StaticResource MicroLabel}" Margin="1,0,0,7"/>
                        <TextBox Text="{Binding SelectedThread.Body, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NotesField}" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" MinHeight="170"/>
                    </StackPanel>

                    <Border Grid.Row="3" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,22,0,0">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,11" LastChildFill="True">
                                <Button DockPanel.Dock="Right" Content="＋ Add files" Click="AddFiles_Click" Style="{StaticResource NotesSecondaryButton}" Padding="14,7" MinWidth="104"/>
                                <StackPanel><TextBlock Text="ATTACHMENTS" Style="{StaticResource MicroLabel}"/><TextBlock Text="PDFs, Word files, slides, images, code, and more" Foreground="{DynamicResource TextSecondary}" FontSize="10" Margin="0,4,12,0"/></StackPanel>
                            </DockPanel>
                            <ItemsControl ItemsSource="{Binding SelectedThread.Attachments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource FieldBackground}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8" Padding="12,9" Margin="0,3">
                                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                <TextBlock Text="📄" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <TextBlock Grid.Column="1" Text="{Binding FileName}" Foreground="{DynamicResource TextPrimary}" FontSize="12" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="2" Text="{Binding SizeLabel}" Foreground="{DynamicResource TextSecondary}" FontSize="10" VerticalAlignment="Center" Margin="10,0"/>
                                                <Button Grid.Column="3" Content="Open" Tag="{Binding}" Click="OpenAttachment_Click" Style="{StaticResource NotesSecondaryButton}" Padding="10,5" FontSize="10"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <StackPanel Grid.Row="4" Margin="0,24,0,0">
                        <DockPanel Margin="1,0,1,10" LastChildFill="True"><TextBlock DockPanel.Dock="Right" Text="Forum-style replies" Foreground="{DynamicResource TextSecondary}" FontSize="10"/><TextBlock Text="DISCUSSION" Style="{StaticResource MicroLabel}"/></DockPanel>
                        <ItemsControl ItemsSource="{Binding SelectedThread.Replies}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="11" Padding="14" Margin="0,0,0,8">
                                        <StackPanel>
                                            <DockPanel><TextBlock Text="{Binding Author}" Foreground="{DynamicResource Mint}" FontSize="11" FontWeight="SemiBold"/><TextBlock DockPanel.Dock="Right" Text="{Binding CreatedAt, StringFormat={}{0:g}}" Foreground="{DynamicResource SectionText}" FontSize="9"/></DockPanel>
                                            <TextBlock Text="{Binding Content}" Foreground="{DynamicResource TextPrimary}" TextWrapping="Wrap" FontSize="12" Margin="0,7,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <Border Grid.Row="5" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="12" Padding="14" Margin="0,4,0,0">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBox x:Name="ReplyTextBox" Style="{StaticResource NotesField}" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="58" ToolTip="Add a question, clarification, correction, or summary"/>
                            <Button Grid.Column="1" Content="Post reply" Click="PostReply_Click" Style="{StaticResource NotesButton}" Margin="10,0,0,0" Width="104" VerticalAlignment="Stretch"/>
                        </Grid>
                    </Border>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
