// [term, category, related week, definition, purpose, example, mistake, recall question]
module.exports = [
  ['Data type', 'Values & input', 1, 'A category that tells C# what kind of value something holds and which operations are allowed.', 'Types help the compiler catch incompatible operations before running your program.', 'int age = 21;\nstring name = "Damian";\nbool isStudent = true;', 'A number written inside quotes, such as "21", is text, not an int.', 'What types would you choose for a name, item count and yes/no answer?'],
  ['Variable', 'Values & input', 1, 'A named storage location with a type. Its current value can usually change.', 'Keep information so later statements can read or update it.', 'int score = 0;\nscore = score + 10;', 'Declaring a local variable without assigning a value does not let you read it immediately.', 'Which part is the type, name and initial value in int score = 0;?'],
  ['Value and literal', 'Values & input', 1, 'A value is a piece of data. A literal is a value written directly in your source code.', 'Distinguish actual data from the name used to access it.', 'int count = 3; // 3 is a literal\nint next = count + 1;', 'The variable name count is not the value 3; the value can change.', 'In string city = "Warsaw";, which part is the literal?'],
  ['Declaration and assignment', 'Values & input', 1, 'A declaration introduces a variable. Assignment stores a value in it. Initialization is its first assignment.', 'Separate creating a variable from changing its value.', 'int total; // declaration\ntotal = 5; // assignment\nint count = 2; // declaration with initialization', 'The = operator assigns; == compares values.', 'Does total = 8; create another variable if total already exists?'],
  ['String', 'Values & input', 1, 'The string type represents a sequence of characters: text. Strings are immutable, meaning their contents cannot be changed in place.', 'Represent names, messages and console input.', 'string message = "Hello";\nmessage = message + "!"; // assigns a new string', 'String methods such as Trim return a result; they do not modify the original string.', 'What does "".Length return?'],
  ['Character (char)', 'Values & input', 1, 'A char represents one UTF-16 code unit. Many familiar characters fit in one char; some symbols, such as many emoji, need more than one.', 'Represent a simple operator or letter when a whole string is unnecessary.', "char operation = '+';\nstring text = Console.ReadLine() ?? \"\";\nif (text.Length == 1) operation = text[0];", 'ReadLine returns string or null, not char. Single quotes are for char literals; double quotes are for strings.', 'Why can char choice = Console.ReadLine(); not compile?'],
  ['Null', 'Values & input', 1, 'Null means no object reference is present. It is not the same as empty text, zero or false.', 'Represent absence and handle missing input explicitly.', 'string? input = Console.ReadLine();\nif (input is null) return; // input stream ended', 'Pressing Enter on an empty console line usually returns "", not null.', 'Why should a prompt loop stop if its input stream has closed?'],
  ['Nullable reference (string?)', 'Values & input', 1, 'The ? annotation tells the compiler that a reference may be null when nullable analysis is enabled.', 'Get warnings when you might use missing data without checking it.', 'string? input = Console.ReadLine();\nif (input is not null) Console.WriteLine(input.Length);', 'string? does not validate text or automatically prevent runtime errors. A plain string annotation is not a runtime null barrier either.', 'Can a string? variable contain ordinary nonempty text?'],
  ['Null-coalescing operator (??)', 'Values & input', 1, 'Use the left value if it is not null; otherwise evaluate and use the right value.', 'Provide a deliberate fallback for missing data.', 'string input = Console.ReadLine() ?? "";', 'The fallback "" contains zero characters, not one space. ?? does not replace whitespace or other non-null values.', 'What result does "   " ?? "fallback" produce?'],
  ['Empty string and whitespace', 'Values & input', 1, 'An empty string has length zero. Whitespace is characters such as spaces, tabs or line breaks. Neither is automatically null.', 'Decide whether input contains meaningful text.', 'string empty = "";\nstring spaces = "   ";\nbool invalid = string.IsNullOrWhiteSpace(spaces); // true', 'if and while do not reject empty strings by themselves; you must supply a condition that checks them.', 'How does IsNullOrEmpty differ from IsNullOrWhiteSpace for "   "?'],
  ['Validation', 'Values & input', 1, 'Checking whether data meets the rules your program requires.', 'Reject bad input before performing an operation.', 'if (!int.TryParse(input, out int age) || age < 1 || age > 100)\n{\n    Console.WriteLine("Enter a whole number from 1 to 100.");\n}', 'Successful conversion is not proof of valid business data: -5 is an int but may be an invalid age.', 'Name one conversion rule and one business rule for an order quantity.'],
  ['Parsing and TryParse', 'Values & input', 1, 'Parsing reads text as a particular kind of value. TryParse reports success with a bool and supplies the parsed value through out.', 'Handle expected input mistakes without using exceptions as the normal retry mechanism.', 'if (int.TryParse("42", out int number))\n    Console.WriteLine(number + 1);', 'Do not ignore the returned bool. Decimal parsing also depends on number format and culture.', 'What should your app do when TryParse receives "abc"?'],
  ['Numeric types', 'Values & input', 2, 'Types for numbers include int for whole numbers, double for binary floating-point approximations and decimal for decimal arithmetic.', 'Choose a representation appropriate to the calculation.', 'int count = 3;\ndouble distance = 2.5;\ndecimal price = 19.99m;\ndouble average = 5 / 2.0; // 2.5', '5 / 2 uses integer division and yields 2. decimal still needs a rounding policy for money.', 'Why does double result = 5 / 2; store 2 rather than 2.5?'],
  ['Boolean and condition', 'Control flow', 1, 'A bool is either true or false. A condition is an expression evaluated to choose a branch or control repetition.', 'Make decisions using comparisons and logical rules.', 'bool allowed = age >= 18 && age <= 100;\nif (allowed) Console.WriteLine("Allowed");', 'C# does not treat an empty string as false. Use an explicit comparison or checking method.', 'What must be true for a condition joined with && to be true?'],
  ['Operator', 'Control flow', 2, 'Syntax that performs an operation on one or more values, such as +, ==, && or ??.', 'Calculate, compare, combine conditions or choose fallback values.', 'int sum = 2 + 3;\nbool equal = sum == 5;\nbool valid = equal && sum > 0;', 'Do not confuse assignment = with comparison ==. Parentheses make intended precedence clearer.', 'How is + different for numbers and strings?'],
  ['If and switch', 'Control flow', 2, 'If selects code based on conditions. Switch selects branches based on a value or matching pattern.', 'Express alternatives without running every branch.', 'switch (operation)\n{\n    case "+": Console.WriteLine(2 + 3); break;\n    default: Console.WriteLine("Unsupported operator"); break;\n}', 'Always consider unmatched or invalid input rather than silently doing nothing.', 'When might a switch make a calculator easier to read?'],
  ['Loop (while / for / foreach)', 'Control flow', 1, 'A loop repeats statements. While checks a condition, for organizes counted repetition, and foreach visits a sequence.', 'Retry input or process multiple values without duplicating code.', 'while (true)\n{\n    string? input = Console.ReadLine();\n    if (input is null) break;\n    if (string.IsNullOrWhiteSpace(input)) continue;\n    Console.WriteLine(input);\n    break;\n}', 'An unchanged condition or repeated null input can cause an infinite loop. Identify how every path makes progress or exits.', 'Which line retries blank input, and which exits on end-of-input?'],
  ['Break, continue and return', 'Control flow', 2, 'Break exits the nearest loop or switch; continue starts the next loop iteration; return exits the current method.', 'Control exactly which code runs next.', 'for (int i = 0; i < 10; i++)\n{\n    if (i == 2) continue;\n    if (i == 5) break;\n    Console.WriteLine(i);\n}', 'Break inside an inner loop does not exit every surrounding loop or the entire method.', 'Which numbers does the example print?'],
  ['Scope', 'Methods & design', 3, 'The region of source code where a name can be used.', 'Keep variables near their purpose and avoid accidental name conflicts.', 'if (true)\n{\n    int local = 1;\n    Console.WriteLine(local);\n}\n// local is not accessible here', 'A variable declared inside a loop body is not generally available after that block.', 'Where would you declare a total used both during and after a loop?'],
  ['Function and method', 'Methods & design', 3, 'A function is a reusable unit of behavior. In C#, a function declared as a member of a type is a method. C# also supports local functions and lambdas.', 'Give a repeated operation a clear name, inputs and result.', 'static int Add(int left, int right)\n{\n    return left + right;\n}\n// Call from a suitable context: int total = Add(2, 3);', 'Writing a method declaration does not execute its body; you must call it.', 'What task from your calculator could become a method?'],
  ['Parameter and argument', 'Methods & design', 3, 'A parameter is a named input in a method declaration. An argument is the actual expression supplied when calling it.', 'Reuse one method with different inputs.', 'static int Double(int number) => number * 2;\n// number is the parameter. In Double(5), 5 is the argument.', 'Changing an ordinary value-type parameter does not change the caller’s variable.', 'Identify the parameter and argument in a greeting method and its call.'],
  ['Return value and void', 'Methods & design', 3, 'A return value is data sent back to the caller. Void means the method returns no value.', 'Separate computing a result from displaying it.', 'static int Square(int n) => n * n;\nstatic void PrintSquare(int n) => Console.WriteLine(Square(n));', 'Printing a number does not return that number to the caller.', 'Which method is easier to reuse in a test that checks the numeric result?'],
  ['Overloading', 'Methods & design', 3, 'Giving methods the same name but different parameter lists. The compiler selects a matching overload for the call.', 'Offer related ways to perform an operation.', 'static int Add(int a, int b) => a + b;\nstatic decimal Add(decimal a, decimal b) => a + b;', 'Changing only a return type does not create a valid overload.', 'Which overload does Add(1m, 2m) select?'],
  ['Class', 'Objects & OOP', 5, 'A reference type that defines the data and behavior its objects can have.', 'Model related state and operations together.', 'class Order\n{\n    public int Id { get; set; }\n    public decimal Total { get; set; }\n}', 'A class definition is not itself one particular order. It defines the type used to create orders.', 'What properties might a Customer class need?'],
  ['Object and instance', 'Objects & OOP', 5, 'An object is an instance of a type. A class can have many separate instances with their own state.', 'Represent individual entities such as two different orders.', 'Order first = new Order { Id = 1 };\nOrder second = new Order { Id = 2 };', 'Order second = first; copies the reference, not the whole object. Both variables then refer to the same instance.', 'If two variables refer to the same order, what happens when one changes its Total?'],
  ['Field and property', 'Objects & OOP', 5, 'A field stores data in a type. A property exposes access through get and/or set accessors and can enforce rules.', 'Keep implementation details private while providing a clear public API.', 'class Counter\n{\n    private int count;\n    public int Count => count;\n    public void Increment() => count++;\n}', 'An automatic property with a public setter does not automatically validate assigned values.', 'Why can callers read Count but not assign it in this example?'],
  ['Constructor', 'Objects & OOP', 5, 'Special code that runs when an instance is created. It has the type’s name and no return type.', 'Put a new object into a valid initial state.', 'class Customer\n{\n    public string Name { get; }\n    public Customer(string name)\n    {\n        if (string.IsNullOrWhiteSpace(name)) throw new ArgumentException("Name required");\n        Name = name;\n    }\n}', 'A constructor is not declared with void. Consider validating required inputs there.', 'What state must an order item have immediately after construction?'],
  ['Encapsulation', 'Objects & OOP', 5, 'Keeping state and behavior together and controlling how outside code can access or change that state.', 'Protect business rules rather than relying on every caller to remember them.', 'class Basket\n{\n    public int Count { get; private set; }\n    public void AddItem() => Count++;\n}', 'Making fields private helps, but exposing unrestricted setters can still allow invalid state.', 'How does AddItem protect Count better than a public field?'],
  ['Access modifier', 'Objects & OOP', 5, 'A keyword such as public, private, protected or internal that controls where a member or type is accessible.', 'Expose only the operations callers need.', 'class Account\n{\n    private decimal balance;\n    public decimal Balance => balance;\n}', 'Private is a code-access boundary, not encryption or a substitute for web authorization.', 'Who can directly change the private balance field?'],
  ['Static', 'Objects & OOP', 5, 'A static member belongs to the type rather than to a particular instance.', 'Use operations that do not need per-object state, such as a pure calculation helper.', 'static class Calculations\n{\n    public static int Square(int n) => n * n;\n}', 'Mutable static data is shared; treating it as a separate value per object can introduce bugs.', 'Do you need new Calculations() to call Square?'],
  ['Composition', 'Objects & OOP', 6, 'Building an object from other objects: a has-a relationship.', 'Represent an order that contains items without pretending an order is an item.', 'class Order\n{\n    private readonly List<OrderItem> items = new();\n}', 'Do not use inheritance merely to reuse a few lines when one object should contain another.', 'Does an order have items, or is an order a kind of item?'],
  ['Inheritance', 'Objects & OOP', 7, 'Defining a derived type that extends a base type: an is-a relationship.', 'Share a suitable contract and behavior across related types.', 'class Animal { public virtual string Speak() => "Sound"; }\nclass Dog : Animal { public override string Speak() => "Woof"; }', 'A subclass should remain usable where its base type is expected. Inheritance is not the default solution for all reuse.', 'Why might Order : OrderItem be a misleading model?'],
  ['Polymorphism and overriding', 'Objects & OOP', 7, 'Polymorphism lets code use a shared contract while different objects provide different behavior. Overriding supplies a derived implementation of a virtual or abstract member.', 'Let callers work with capabilities instead of hard-coding every concrete type.', 'Animal animal = new Dog();\nConsole.WriteLine(animal.Speak()); // Woof, using classes above', 'Overriding is not overloading: overloading changes the parameter list; overriding changes inherited behavior.', 'Why does a base-typed variable still call Dog.Speak?'],
  ['Abstraction and abstract class', 'Objects & OOP', 7, 'Abstraction exposes the important operations while hiding details. An abstract class cannot be instantiated directly and may require derived implementations.', 'Describe a common idea that is incomplete by itself.', 'abstract class Shape\n{\n    public abstract double Area();\n}', 'Abstraction is not just the abstract keyword; a well-named ordinary method can also hide detail.', 'Why can you not write new Shape() here?'],
  ['Interface', 'Objects & OOP', 7, 'A type that defines a contract implementing types agree to provide.', 'Separate callers from replaceable implementations, especially at useful boundaries.', 'interface IOrderStore\n{\n    void Save(Order order);\n}', 'Not every class needs a matching interface. Introduce one for a real boundary or interchangeable behavior.', 'How could an in-memory store and file store satisfy the same contract?'],
  ['Value type and reference type', 'Objects & OOP', 6, 'Assignment copies the value of a value type, such as int or a struct. For a reference type, assignment copies a reference to an object.', 'Predict what changes are shared between variables.', 'int a = 1;\nint b = a;\nb = 2; // a is still 1\nOrder x = new Order();\nOrder y = x; // same order object', '“Value types live on the stack, reference types on the heap” is not a reliable definition; behavior is the key distinction.', 'Does assigning a list to another variable make an independent list?'],
  ['Enum', 'Objects & OOP', 6, 'A value type containing named constants backed by an integral type.', 'Express a small set of named states more clearly than magic numbers.', 'enum OrderStatus { Draft, Submitted, Shipped, Cancelled }', 'Enums do not enforce legal state transitions and can contain unnamed numeric values.', 'Can using an enum alone prevent a cancelled order from being submitted?'],
  ['Collection, array and List', 'Collections', 4, 'A collection groups values. An array has a fixed length; List<T> can grow or shrink.', 'Store and process many items under one variable name.', 'int[] fixedScores = { 10, 20 };\nList<int> scores = new() { 10, 20 };\nscores.Add(30);', 'Indices start at zero. The last valid array index is Length - 1, not Length.', 'When would you choose a list rather than an array?'],
  ['Dictionary and set', 'Collections', 10, 'Dictionary<TKey,TValue> maps unique keys to values. HashSet<T> stores distinct values for membership checks.', 'Find an order by ID or avoid duplicate tags.', 'Dictionary<int, string> names = new() { [1] = "Damian" };\nif (names.TryGetValue(1, out string? name)) Console.WriteLine(name);\nHashSet<string> tags = new() { "C#", "C#" };', 'Dictionary keys must be unique; accessing a missing key with [] can throw.', 'Which collection would you choose to track distinct visited IDs?'],
  ['Generics', 'Collections', 9, 'Type parameters let a type or method work with different specified types while retaining compile-time checks.', 'Reuse collection or method logic without treating every value as object.', 'List<int> scores = new();\nList<string> names = new();', 'T is a placeholder type name, not a magic value or a way to ignore type rules.', 'What does the int in List<int> restrict?'],
  ['Lambda', 'Collections', 9, 'A compact anonymous function written using =>.', 'Supply a small operation or condition to another method.', 'int[] scores = { 40, 70, 90 };\nvar passed = scores.Where(score => score >= 50);', 'The parameter name score represents one item at a time, not the whole collection.', 'What does score => score >= 50 return for one score?'],
  ['LINQ and deferred execution', 'Collections', 9, 'LINQ provides query operations such as Where and Select. Many queries execute when enumerated rather than when first declared.', 'Filter, transform and summarize data in readable steps.', 'List<int> numbers = new() { 1, 2 };\nvar query = numbers.Where(n => n > 1);\nnumbers.Add(3);\nvar snapshot = query.ToList(); // contains 2 and 3', 'An in-memory query and a database-translated query may behave differently. Re-enumeration can repeat work.', 'Why does the snapshot include 3?'],
  ['Algorithm and complexity', 'Collections', 10, 'An algorithm is a sequence of steps to solve a problem. Complexity describes how its resource needs grow with input size.', 'Compare approaches before tuning small details.', '// Checking each item once is O(n).\nforeach (int score in scores) Console.WriteLine(score);', 'Big O is not an exact time in seconds, and a short method can still do expensive work.', 'How does doubling the list size affect a single linear scan?'],
  ['Exception', 'Errors & testing', 8, 'An object describing a failure that interrupts normal control flow until handled.', 'Signal and handle failures such as inaccessible files at a suitable boundary.', 'try\n{\n    string text = File.ReadAllText("orders.json");\n}\ncatch (IOException ex)\n{\n    Console.WriteLine(ex.Message);\n}', 'Do not catch every exception and silently report success or overwrite unreadable saved data.', 'Why is TryParse preferable to catching bad numeric input repeatedly?'],
  ['Debugging and breakpoint', 'Errors & testing', 2, 'Debugging is investigating why actual behavior differs from expected behavior. A breakpoint pauses execution so you can inspect state.', 'Understand the cause of a bug instead of changing code at random.', 'int total = 0;\nfor (int i = 1; i <= 3; i++)\n{\n    total += i; // place a breakpoint here\n}', 'A program that compiles can still contain a logic bug. Inspect inputs and intermediate values.', 'What values of i and total do you expect on the second iteration?'],
  ['Unit test', 'Errors & testing', 7, 'An automated check of a small unit of behavior, commonly a method or class, isolated from slow external systems.', 'Protect business rules and make changes safer.', '[Fact]\npublic void AddingTwoNumbersReturnsTheirSum()\n{\n    Assert.Equal(5, Calculations.Add(2, 3));\n}\n// Requires xUnit and your own Calculations.Add implementation.', 'A high test count is not proof of good coverage. Check meaningful boundaries and failures.', 'Which test would catch allowing a quantity of zero?'],
  ['Integration test', 'Errors & testing', 19, 'A check that multiple components work together, such as your API and database.', 'Find issues that isolated unit tests cannot reveal.', '// Arrange a test database.\n// POST a valid order through the API.\n// GET it and assert its stored items and total.', 'An in-memory fake cannot prove all behavior of a real SQL Server database.', 'What failure could an integration test catch that a calculation unit test cannot?'],
  ['Refactoring', 'Errors & testing', 4, 'Changing the internal structure of working code without deliberately changing its observable behavior.', 'Make code easier to understand and extend.', '// Before: repeated numeric input loops.\n// After: one ReadValidNumber helper called in each place.', 'Adding a feature is not merely refactoring. Tests help check behavior remains the same.', 'What tests would you run after extracting your calculator’s input helper?'],
  ['Task, async and await', 'Async & resources', 12, 'Task represents an operation that may finish later. Async enables await in a method; await lets it pause until completion without blocking a thread during an incomplete asynchronous wait.', 'Keep applications responsive and avoid tying up threads during I/O.', 'static async Task<string> FetchAsync(HttpClient client, string url)\n{\n    return await client.GetStringAsync(url);\n}', 'Async does not automatically create a thread or make CPU calculations faster. Avoid async void except where required for event handlers.', 'What is your method waiting for during an HTTP request?'],
  ['Cancellation token', 'Async & resources', 12, 'A cooperative signal requesting that an operation stop.', 'Stop unnecessary work when a user cancels or a request ends.', 'static Task<string> FetchAsync(HttpClient client, string url, CancellationToken token)\n    => client.GetStringAsync(url, token);', 'Cancellation requests do not forcibly terminate arbitrary code. The operation must observe the token.', 'Why should an API pass the request cancellation token to database calls?'],
  ['Disposal and using', 'Async & resources', 8, 'Disposal releases resources through IDisposable or IAsyncDisposable. A using statement/declaration arranges disposal when its scope ends.', 'Close files and other resources reliably, including on exceptions.', 'using var reader = new StreamReader("orders.json");\nstring content = reader.ReadToEnd();', 'A using directive imports a namespace; it is a different use of the keyword from using var.', 'When is the reader disposed if an exception leaves this scope?'],
  ['Database and SQL', 'Databases', 13, 'A database manages stored data. SQL is a language used to define, query and modify relational data.', 'Store related customers and orders beyond one app session.', '// SQL example:\n// SELECT Id, Name FROM Customers WHERE Id = @id;', 'Do not build SQL by concatenating user input. Use parameters and appropriate authorization.', 'How is storing an order in a database different from keeping it in a local list?'],
  ['Primary key and foreign key', 'Databases', 13, 'A primary key identifies a row uniquely. A foreign key constrains a relationship to a referenced key.', 'Connect orders to customers and prevent invalid references.', '// Customers: Id is the primary key.\n// Orders: CustomerId references Customers.Id.', 'A foreign key does not automatically fetch related rows, and it is not permission to view them.', 'What should happen if an order references a nonexistent customer?'],
  ['Join', 'Databases', 14, 'A SQL operation combining rows according to a matching condition.', 'Read order and customer information in one query.', '// SELECT o.Id, c.Name\n// FROM Orders o\n// INNER JOIN Customers c ON o.CustomerId = c.Id;', 'One-to-many joins can produce several rows per parent. Check cardinality before summing totals.', 'How does a LEFT JOIN treat a left-side row with no match?'],
  ['Transaction', 'Databases', 15, 'A group of database operations treated as a unit that can be committed or rolled back.', 'Avoid saving only part of a related change.', '// Begin transaction.\n// Insert an order and its items.\n// Commit together, or roll back if any required write fails.', 'Wrapping operations in a transaction does not solve every concurrency issue; isolation and constraints matter.', 'Why should creating an order and its required items be atomic?'],
  ['Index', 'Databases', 16, 'A database structure that helps locate rows efficiently for suitable queries.', 'Speed up common searches after inspecting real query behavior.', '// SQL: CREATE INDEX IX_Orders_CustomerId ON Orders(CustomerId);', 'Indexes use storage and add write cost. An index is not automatically useful for every query.', 'Why would you inspect a query plan before adding several indexes?'],
  ['Entity Framework Core and DbContext', 'Databases', 17, 'EF Core maps .NET objects and queries to a database. DbContext coordinates queries, tracked changes and saving.', 'Access relational data from C# while still understanding the resulting SQL.', '// Within an async method with a configured context:\nvar order = await db.Orders.FindAsync(id);', 'DbContext is not thread-safe. EF Core does not remove the need to learn SQL or inspect expensive queries.', 'Why can fetching every row before filtering be inefficient?'],
  ['Migration', 'Databases', 17, 'A versioned description of database schema changes.', 'Evolve tables reproducibly alongside application code.', '// CLI examples, not C# statements:\n// dotnet ef migrations add AddOrderStatus\n// dotnet ef database update', 'A schema rollback can lose data. Review generated changes and plan backups before destructive migrations.', 'How would adding a required column affect existing rows?'],
  ['HTTP and API', 'Web & delivery', 20, 'HTTP is a request/response protocol. An API is a contract for interacting with software; a web API commonly exposes that contract over HTTP.', 'Let clients request data and operations from your server.', '// GET /orders/42 requests an order.\n// POST /orders creates one according to the API contract.', 'Not every API is a web API. A URL alone does not define validation, permissions or error behavior.', 'What should a client send and receive to create an order?'],
  ['Endpoint and status code', 'Web & delivery', 20, 'An endpoint is an addressable operation, typically identified by an HTTP method and route. A status code communicates the request outcome.', 'Give clients predictable success and failure responses.', '// GET /orders/42 -> 200 when accessible and found\n// Invalid create request -> 400\n// Successful creation -> often 201', 'Returning 200 for every failure hides errors. 401 and 403 have different authentication/authorization meanings.', 'Why are GET /orders and POST /orders different operations?'],
  ['DTO and JSON', 'Web & delivery', 21, 'A DTO is an object shaped for transferring data. JSON is a text format commonly used to serialize that data.', 'Define explicit API inputs and outputs without exposing every database field.', 'record CreateOrderRequest(int CustomerId);\n// JSON: { "customerId": 42 }', 'Do not trust a client-supplied total, owner ID or role just because the JSON parses.', 'Why might your API response differ from its EF entity?'],
  ['Dependency injection', 'Web & delivery', 20, 'Providing the objects a class needs from outside rather than constructing all of them internally.', 'Make dependencies explicit and support appropriate replacement and testing.', 'class OrderService\n{\n    private readonly IOrderStore store;\n    public OrderService(IOrderStore store) => this.store = store;\n}', 'A DI container is a wiring tool, not proof of good design. Lifetimes must fit their dependencies.', 'How does this constructor let a test supply a fake store?'],
  ['Middleware', 'Web & delivery', 22, 'A component in the ASP.NET Core request pipeline that can inspect a request, call the next component and inspect the response.', 'Apply cross-cutting behavior such as logging or authentication.', '// Conceptual order: error handling -> routing ->\n// authentication -> authorization -> endpoint.', 'Pipeline order changes behavior. Authentication should run before authorization checks that rely on it.', 'Why should exception-handling middleware surround later request processing?'],
  ['Authentication and authorization', 'Web & delivery', 25, 'Authentication establishes identity. Authorization decides what that identity may do.', 'Protect resources, including access to each user’s orders.', '// Signed in as user B: authenticated.\n// Reading user A’s private order: must still be denied.', 'Hiding a button or enabling CORS is not server-side authorization.', 'Which check prevents one logged-in user from editing another user’s order?'],
  ['Git, commit and branch', 'Tools & career', 1, 'Git tracks versions of files. A commit records a snapshot; a branch is a movable name for a line of development.', 'Keep meaningful history, try changes and collaborate.', '// CLI, not C#:\n// git status\n// git add Program.cs\n// git commit -m "Retry invalid calculator input"', 'GitHub hosts repositories; it is not Git itself. A local commit is not automatically uploaded.', 'What should you inspect before committing?'],
  ['Folder, project and solution', 'Tools & career', 1, 'A folder groups files. A C# project describes a build, usually in a .csproj file. A solution groups projects, using .sln or .slnx.', 'Organize exercises and point build commands at real paths.', '// From your existing Practice directory, PowerShell:\n// dotnet new console -n input-validation\n// dotnet sln .\\Practice.slnx add .\\input-validation\\input-validation.csproj\n// dotnet run --project .\\input-validation\\input-validation.csproj', 'Adding a project to a solution does not move its folder. Do not insert an active path segment unless it exists.', 'What would you check first after “Project file does not exist”?'],
  ['SDK, runtime and compiler', 'Tools & career', 1, 'The SDK includes development tools. The compiler translates source code and checks language rules. The runtime executes the compiled application.', 'Understand what you need to build versus run .NET programs.', '// CLI: dotnet --list-sdks\n// CLI: dotnet build\n// CLI: dotnet run', 'The programming language C# is not the same thing as the .NET platform or Visual Studio editor.', 'Which component reports a string-to-char assignment compilation error?'],
  ['Markdown and code fence', 'Tools & career', 1, 'Markdown formats documentation. Triple backticks mark a code block in Markdown; a language label such as csharp helps presentation.', 'Show readable code in notes and README files.', '// In Program.cs, include C# only.\nConsole.WriteLine("Hello");\n// Do not paste Markdown fence lines into the source file.', 'The ```csharp and closing ``` markers belong to the document, not to Program.cs.', 'Why would pasting a code fence into Program.cs cause a syntax error?'],
  ['CI and deployment', 'Web & delivery', 29, 'Continuous integration runs automated checks when code changes. Deployment makes a build available in a target environment.', 'Catch failures before release and deliver repeatably.', '// CI: restore -> build -> unit tests -> integration tests\n// Deployment: publish -> configure -> start -> verify', 'A successful build is not proof that a deployed app can connect to its database or enforce permissions.', 'What checks would you run immediately after deployment?'],
  ['Container and Docker Compose', 'Web & delivery', 30, 'A container runs an application in an isolated environment based on an image. Compose describes a group of related containers.', 'Reproduce an API-and-database setup for another developer.', '// Compose can define an API service, a database service,\n// their configuration, network and persistent database volume.', 'Deleting a container can lose data stored only inside it. Persistent volumes and backups serve different purposes.', 'Why should database data live in a persistent volume?']
];
